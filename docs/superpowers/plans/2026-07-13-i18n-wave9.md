# Wave 9: 6-locale expansion (ja-JP + ko-KR) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-KILL: superpowers:subagent-driven-development or superpowers:executing-plans.
> **Reference spec:** `docs/superpowers/specs/2026-07-13-i18n-wave9.md`
> **Base branch:** feature/stability-wave8
> **New branch:** feature/i18n-wave9

## Task 1: 创建分支 + commit spec/plan

```bash
cd meetily
git checkout feature/stability-wave8
git checkout -b feature/i18n-wave9
git add docs/superpowers/specs/2026-07-13-i18n-wave9.md \
        docs/superpowers/plans/2026-07-13-i18n-wave9.md
git commit -m "docs(i18n): add Wave 9 spec + implementation plan (PR-35~37)"
```

## Task 2: PR-35 6 locale 基础设施

### 2.1 `frontend/src/i18n/config.ts`

```ts
export const LOCALES = ["en-US", "en-GB", "zh-CN", "zh-TW", "ja-JP", "ko-KR"] as const;
export type Locale = (typeof LOCALES)[number];
export const DEFAULT_LOCALE: Locale = "en-US";

export function resolveLocaleFromOs(osLocale: string | null | undefined): Locale {
  if (!osLocale) return DEFAULT_LOCALE;
  const lower = osLocale.toLowerCase();
  if (lower === "zh-tw" || lower === "zh-hk" || lower === "zh-mo" || lower === "zh-hant") return "zh-TW";
  if (lower.startsWith("zh")) return "zh-CN";
  if (lower === "en-gb") return "en-GB";
  if (lower.startsWith("en")) return "en-US";
  if (lower.startsWith("ja")) return "ja-JP";
  if (lower.startsWith("ko")) return "ko-KR";
  return DEFAULT_LOCALE;
}
```

### 2.2 `frontend/src/i18n/request.ts`

- 增加 14 个 import(7 ja-JP + 7 ko-KR)
- `Messages` type 加 2 个 namespace
- `MESSAGES` 字典加 2 个 locale 入口,内容 = en-US 镜像

### 2.3 创建 14 个新 JSON 文件

```bash
mkdir -p frontend/locales/ja-JP frontend/locales/ko-KR
for f in common recording transcript summary settings errors sidebar; do
  cp frontend/locales/en-US/$f.json frontend/locales/ja-JP/$f.json
  cp frontend/locales/en-US/$f.json frontend/locales/ko-KR/$f.json
done
```

### 2.4 settings.json 加 2 个 key(两侧都加)

en-US 和 en-GB:
- `preference.locale_name_ja_jp = "日本語"`
- `preference.locale_name_ko_kr = "한국어"`

zh-CN 和 zh-TW:
- `preference.locale_name_ja_jp = "日本語"`
- `preference.locale_name_ko_kr = "한국어"`

### 2.5 Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add -A
git commit -m "feat(i18n): extend locale matrix to 6 languages (en-US/en-GB/zh-CN/zh-TW/ja-JP/ko-KR) (PR-35)"
```
## Task 3: PR-36 ja-JP 全量翻译

逐文件简体→日语转换。优先用 Wave 7 zh-TW 词表经验 + 日本 IT 圈用语:

| 英文 | 日语 |
|------|------|
| Settings | 設定 |
| Recording | 録音 |
| Transcript | 文字起こし |
| Summary | 要約 |
| Microphone | マイク |
| System Audio | システム音声 |
| Default | デフォルト |
| Save | 保存 |
| Discard | 破棄 |
| Delete | 削除 |
| Edit | 編集 |
| Confirm | 確認 |
| Cancel | キャンセル |
| Enable | 有効 |
| Disable | 無効 |
| Close | 閉じる |
| Open | 開く |
| Loading | 読み込み中 |
| Error | エラー |
| Failed | 失敗 |
| Success | 成功 |
| Model | モデル |
| Provider | プロバイダー |
| Recover | 復元 |
| Pause | 一時停止 |
| Resume | 再開 |
| Processing | 処理中 |
| Generating | 生成中 |
| Recovery | 復元 |
| Orphan | 孤立 |
| Checkpoint | チェックポイント |
| Chain | チェーン |
| Failover | フェイルオーバー |
| Primary | プライマリ |
| Fallback | フォールバック |
| Provider chain | プロバイダーチェーン |

术语例外(保留英文):Whisper / Parakeet / Ollama / OpenRouter / Claude / Groq / BlackHole / Meetily / Beta / JSON / Tauri / API / CUDA / Metal / CPU

实施策略:
```bash
# 从 en-US 复制 → frontend/locales/ja-JP/*.json
# 逐文件做 key-by-key 字符串替换
# 保留占位符 {count} {duration} 等不变
```

### Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add frontend/locales/ja-JP/
git commit -m "feat(i18n): ja-JP full conversion (PR-36)"
```

## Task 4: PR-37 ko-KR 全量翻译

逐文件简体→韩语转换。优先用 Wave 7 zh-TW 词表经验 + 韩国 IT 圈用语:

| 英文 | 韩语 |
|------|------|
| Settings | 설정 |
| Recording | 녹음 |
| Transcript | 변환 |
| Summary | 요약 |
| Microphone | 마이크 |
| System Audio | 시스템 오디오 |
| Default | 기본 |
| Save | 저장 |
| Discard | 폐기 |
| Delete | 삭제 |
| Edit | 편집 |
| Confirm | 확인 |
| Cancel | 취소 |
| Enable | 활성화 |
| Disable | 비활성화 |
| Close | 닫기 |
| Open | 열기 |
| Loading | 로드 중 |
| Error | 오류 |
| Failed | 실패 |
| Success | 성공 |
| Model | 모델 |
| Provider | 공급자 |
| Recover | 복구 |
| Pause | 일시 정지 |
| Resume | 재개 |
| Processing | 처리 중 |
| Generating | 생성 중 |
| Recovery | 복구 |
| Orphan | 고아 |
| Checkpoint | 체크포인트 |
| Chain | 체인 |
| Failover | 장애 조치 |
| Primary | 기본 |
| Fallback | 대체 |
| Provider chain | 공급자 체인 |

术语例外(保留英文):同 ja-JP

### Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add frontend/locales/ko-KR/
git commit -m "feat(i18n): ko-KR full conversion (PR-37)"
```

## Task 5: 收尾

```bash
cd meetily
git log --oneline feature/stability-wave8..feature/i18n-wave9
```

期望:4 commit (1 docs + 3 functional)。

## Self-Review Checklist

- [ ] 6 个 locale 全部纳入 `LOCALES` 数组
- [ ] `resolveLocaleFromOs` 支持 ja/ko 前缀
- [ ] `check-i18n` 跨 6 locale 验证通过
- [ ] `UiLanguagePicker` 自动出现 6 个选项
- [ ] ja-JP 仅改 value 不改 key
- [ ] ko-KR 仅改 value 不改 key
- [ ] glossary 81 术语对齐
- [ ] 三道闸全过
- [ ] en-US / en-GB / zh-CN / zh-TW 不退化
