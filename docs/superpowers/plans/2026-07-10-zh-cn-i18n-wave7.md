# Wave 7: multi-locale expansion (en-GB + zh-TW) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-KILL: superpowers:subagent-driven-development or superpowers:executing-plans.
> **Reference spec:** `docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave7.md`

## Task 1: Create branch + commit spec/plan

```bash
cd meetily
git checkout feature/i18n-wave6
git checkout -b feature/i18n-wave7
git add docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave7.md \
        docs/superpowers/plans/2026-07-10-zh-cn-i18n-wave7.md
git commit -m "docs(i18n): add Wave 7 spec + implementation plan (PR-28~30)"
```

## Task 2: PR-28 multi-locale infrastructure

### 2.1 `frontend/src/i18n/config.ts`

```ts
export const LOCALES = ["en-US", "en-GB", "zh-CN", "zh-TW"] as const;
export type Locale = (typeof LOCALES)[number];
export const DEFAULT_LOCALE: Locale = "en-US";

export function resolveLocaleFromOs(osLocale: string | null | undefined): Locale {
  if (!osLocale) return DEFAULT_LOCALE;
  const lower = osLocale.toLowerCase();
  if (lower === "zh-tw" || lower === "zh-hk" || lower === "zh-mo" || lower === "zh-hant") return "zh-TW";
  if (lower.startsWith("zh")) return "zh-CN";
  if (lower === "en-gb") return "en-GB";
  if (lower.startsWith("en")) return "en-US";
  return DEFAULT_LOCALE;
}

export function isSupportedLocale(value: string): value is Locale {
  return (LOCALES as readonly string[]).includes(value);
}
```

### 2.2 `frontend/src/i18n/request.ts`

- 增加 14 个 import(7 en-GB + 7 zh-TW)
- `Messages` type 加 2 个 namespace(typeof commonEn 等)
- `MESSAGES` 字典加 2 个 locale 入口,内容 = en-US 镜像

### 2.3 `frontend/scripts/check-i18n.ts`

替换硬编码:
```ts
import { LOCALES } from "../i18n/config";
const LOCALES_DIR = ...
```

### 2.4 创建 14 个新 JSON 文件

```bash
mkdir -p frontend/locales/en-GB frontend/locales/zh-TW
for f in common recording transcript summary settings errors sidebar; do
  cp frontend/locales/en-US/$f.json frontend/locales/en-GB/$f.json
  cp frontend/locales/en-US/$f.json frontend/locales/zh-TW/$f.json
done
```

> 初始内容 = en-US 全量复制。后续 PR-29/30 精修。

### 2.5 settings.json 加 2 个 key(两侧都加)

en-US:
- `preference.locale_name_en_gb = "English (UK)"`
- `preference.locale_name_zh_tw = "繁體中文"`

zh-CN:
- `preference.locale_name_en_gb = "English (UK)"`
- `preference.locale_name_zh_tw = "繁體中文"`

### 2.6 Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add -A
git commit -m "feat(i18n): extend locale matrix to 4 languages (en-US/en-GB/zh-CN/zh-TW) (PR-28)"
```

## Task 3: PR-29 en-GB spellings

逐 JSON 校对拼写。重点 keys:

| File | en-US | en-GB |
|------|-------|-------|
| common.json | (无拼写差异) | (同) |
| errors.json | "model" (无) | (同) |
| sidebar.json | (无拼写差异) | (同) |
| recording.json | (无拼写差异) | (同) |
| transcript.json | (无拼写差异) | (同) |
| summary.json | "auto-detect" 描述 | "auto-detect" 同 |
| settings.json | description 类文案 | 校对 color / organize / behavior 等 |

实际改动预计 <30 keys(因为大部分 UI 文案是 1-2 词的术语,英美无差)。

### Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add frontend/locales/en-GB/
git commit -m "feat(i18n): en-GB spelling polish (PR-29)"
```

## Task 4: PR-30 zh-TW traditional Chinese

逐文件简体→繁体转换。优先用 spec 里的词表;对未列出的字符做通用转换(简→繁)。

策略:
1. 从 en-GB 复制(已经是 en-GB 精修版)→ `frontend/locales/zh-TW/*.json`
2. 对每个 zh-TW 文件批量替换 key value 中的简体字符为繁体

实施:
```bash
# 用 node 脚本对每个文件做转换
node scripts/convert-to-tw.js frontend/locales/en-GB/common.json frontend/locales/zh-TW/common.json
# 转换表见 spec
```

> 实施细节:用简单的 key-by-key 字符串替换,避免破坏占位符 `{name}` `{version}` 等。

### Verify + commit

```bash
cd frontend
pnpm run check:i18n && pnpm test:i18n && pnpm build
cd ..
git add frontend/locales/zh-TW/
git commit -m "feat(i18n): zh-TW traditional Chinese full conversion (PR-30)"
```

## Task 5: 收尾

```bash
cd meetily
git log --oneline feature/i18n-wave6..feature/i18n-wave7
```

期望:4 commit (1 docs + 3 functional)。

## Self-Review Checklist

- [ ] 4 个 locale 全部纳入 `LOCALES` 数组
- [ ] `check-i18n.ts` 用 LOCALES 不用硬编码
- [ ] `UiLanguagePicker` 自动出现 4 个选项(无需改代码)
- [ ] en-GB 仅改拼写不改语义
- [ ] zh-TW 全部繁体,术语与 glossary 一致
- [ ] 三道闸全过
- [ ] en-US / zh-CN 不退化
