# Release v0.5.0 Plan

## Step 1. Bump version in 3 files

- `frontend/package.json`: `"version": "0.4.0"` → `"0.5.0"`
- `frontend/src-tauri/Cargo.toml`: `version = "0.4.0"` → `version = "0.5.0"`
- `frontend/src-tauri/tauri.conf.json`: `"version": "0.4.0"` → `"0.5.0"`

Verify with grep:

```bash
grep -n '"version"\|version =' frontend/package.json \
  frontend/src-tauri/Cargo.toml \
  frontend/src-tauri/tauri.conf.json
```

All three must show `0.5.0`.

## Step 2. CHANGELOG.md

Insert (above the current `[Unreleased]` heading, around line 12):

```
## [Unreleased]

### Added
- (none yet)

### Changed
- (none yet)

### Fixed
- (none yet)

---
```

Then change line 12 `## [Unreleased]` → `## [v0.5.0] - 2026-07-19`.

Append at the bottom of the file (the link-reference block):

```
[v0.5.0]: https://github.com/LSY1105/meetily/releases/tag/v0.5.0
```

## Step 3. RELEASE_NOTES_v0.5.0.md

Write a new file at the repo root with the bilingual structure from the
spec. Keep the file self-contained; reference CHANGELOG.md for the full
per-PR list rather than duplicating it.

## Step 4. Commit + push + merge

```bash
git add -A
git commit -m "chore(release): v0.5.0"
git push fork feature/release-v0.5.0
```

Web: open PR `feature/release-v0.5.0` → `devtest`, merge via
"Create a merge commit".

## Step 5. Tag (user runs locally after merge)

```bash
git fetch fork
git switch devtest
git pull --ff-only fork devtest
git tag -a v0.5.0 -F - <<'TAGMSG'
v0.5.0 - Wave 25 release

Highlights:
- Streaming LLM transcript postprocess (PR-42-iii)
- Semantic postprocess error codes (PR-42-iv-b, breaking wire format)
- Typed LLMError enum for generate_summary (PR-42-iv-c)
- Rust unit tests for llm_postprocess (PR-42-iv-a)
- Hotword hit-rate panel with SQLite counter (PR-A)
- LLM summary prompts inject hotword glossary (PR-F)
- Weekly ASR benchmark CI (PR-C)

See RELEASE_NOTES_v0.5.0.md and CHANGELOG.md for full details.
TAGMSG
git push fork v0.5.0
git branch -d feature/release-v0.5.0
git push fork --delete feature/release-v0.5.0
git remote prune fork
```

## File budget

| File | + / - |
|---|---|
| package.json | 1 line changed |
| Cargo.toml | 1 line changed |
| tauri.conf.json | 1 line changed |
| CHANGELOG.md | +14 / -1 |
| RELEASE_NOTES_v0.5.0.md | +80 / -0 (new) |
| spec.md | this file's spec |
| plan.md | this file |
| **合计** | **+98 / -3** |
