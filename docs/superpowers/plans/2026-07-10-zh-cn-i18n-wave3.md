# Wave 3: PR-14~17 Migration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Migrate ~60 hardcoded English strings across 5 always-visible components.
**Architecture:** Mechanical `useTranslations()` substitution; no new dependencies.
**Tech Stack:** next-intl 4 (already installed), React 18, TypeScript 5

**Reference spec:** `docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave3.md`

## Task 1: 创建分支 + 提交 spec/plan

- [ ] **Step 1.1**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git checkout feature/i18n-wave2
git checkout -b feature/i18n-wave3
git add docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave3.md docs/superpowers/plans/2026-07-10-zh-cn-i18n-wave3.md
git commit -m "docs(i18n): add Wave 3 spec + implementation plan (PR-14~17)"
```

---

## Task 2: PR-14 Sidebar

**Files:**
- Create: `frontend/locales/en-US/sidebar.json`
- Create: `frontend/locales/zh-CN/sidebar.json`
- Modify: `frontend/src/i18n/request.ts` (add sidebar imports + entries in MESSAGES map)
- Modify: `frontend/src/components/Sidebar/index.tsx`

- [ ] **Step 2.1: 创建 en-US/sidebar.json**

```json
{
  "nav": {
    "home": "Home",
    "meetings": "Meetings",
    "import_audio": "Import Audio",
    "notes": "Meeting Notes",
    "settings": "Settings"
  },
  "meeting_list": {
    "search_placeholder": "Search meeting content...",
    "searching": "Searching...",
    "edit_title_dialog_title": "Edit Meeting Title",
    "edit_title_input_placeholder": "Enter meeting title",
    "save": "Save",
    "cancel": "Cancel"
  },
  "status": {
    "recording_in_progress": "Recording in progress...",
    "start_recording": "Start Recording",
    "import_audio": "Import Audio"
  },
  "brand": "Meetily"
}
```

- [ ] **Step 2.2: 创建 zh-CN/sidebar.json**

```json
{
  "nav": {
    "home": "首页",
    "meetings": "会议",
    "import_audio": "导入音频",
    "notes": "会议笔记",
    "settings": "设置"
  },
  "meeting_list": {
    "search_placeholder": "搜索会议内容...",
    "searching": "搜索中...",
    "edit_title_dialog_title": "编辑会议标题",
    "edit_title_input_placeholder": "请输入会议标题",
    "save": "保存",
    "cancel": "取消"
  },
  "status": {
    "recording_in_progress": "正在录音...",
    "start_recording": "开始录音",
    "import_audio": "导入音频"
  },
  "brand": "Meetily"
}
```

- [ ] **Step 2.3: request.ts 注册 sidebar 命名空间**

在 `frontend/src/i18n/request.ts`：
- 顶部加 `import sidebarEn from "../../locales/en-US/sidebar.json";` 和 `import sidebarZh from "../../locales/zh-CN/sidebar.json";`
- `Messages` 类型加 `sidebar: typeof sidebarEn;`
- `MESSAGES` 字典在 en-US 和 zh-CN 中都加 `sidebar: sidebarEn,` / `sidebar: sidebarZh,`

- [ ] **Step 2.4: 跑 check:i18n**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily\frontend
pnpm run check:i18n
```

**Acceptance:** `i18n check passed`.

- [ ] **Step 2.5: Sidebar/index.tsx 顶部加 useTranslations**

在 `import` 区段加：
```tsx
import { useTranslations } from "next-intl";
```

在 `export function Sidebar()` 函数体顶部加：
```tsx
const t = useTranslations("sidebar");
```

- [ ] **Step 2.6: 替换硬编码字符串**

按以下映射替换：
| 旧 | 新 |
|---|
| `<p>Home</p>` | `<p>{t("nav.home")}</p>` |
| `<p>Import Audio</p>` | `<p>{t("nav.import_audio")}</p>` |
| `<p>Meeting Notes</p>` | `<p>{t("nav.notes")}</p>` |
| `<p>Settings</p>` | `<p>{t("nav.settings")}</p>` |
| `placeholder='Search meeting content...'` | `placeholder={t("meeting_list.search_placeholder")}` |
| `<span className="ml-2 text-xs text-blue-500 animate-pulse">Searching...</span>` | `<span className="ml-2 text-xs text-blue-500 animate-pulse">{t("meeting_list.searching")}</span>` |
| `<span>Meetily</span>` (brand) | `<span>{t("brand")}</span>` |
| `<span>Home</span>` (mobile nav) | `<span>{t("nav.home")}</span>` |
| `<span>Recording in progress...</span>` | `<span>{t("status.recording_in_progress")}</span>` |
| `<span>Start Recording</span>` | `<span>{t("status.start_recording")}</span>` |
| `<span>Import Audio</span>` (mobile) | `<span>{t("nav.import_audio")}</span>` |
| `<span>Settings</span>` (mobile) | `<span>{t("nav.settings")}</span>` |
| `<DialogTitle>Edit Meeting Title</DialogTitle>` | `<DialogTitle>{t("meeting_list.edit_title_dialog_title")}</DialogTitle>` |
| `<h3 className="text-lg font-semibold mb-4">Edit Meeting Title</h3>` | `<h3 className="text-lg font-semibold mb-4">{t("meeting_list.edit_title_dialog_title")}</h3>` |
| `placeholder="Enter meeting title"` | `placeholder={t("meeting_list.edit_title_input_placeholder")}` |

**Acceptance:** build 通过；Sidebar 在 zh-CN 下所有 nav 标签、按钮、placeholder 立刻变中文。

- [ ] **Step 2.7: build + commit**

```bash
pnpm test:i18n && pnpm run check:i18n && pnpm build
git add frontend/locales/en-US/sidebar.json frontend/locales/zh-CN/sidebar.json frontend/src/i18n/request.ts frontend/src/components/Sidebar/index.tsx
git commit -m "feat(i18n): migrate Sidebar strings (PR-14)"
```

---

## Task 3: PR-15 RecordingControls

**Files:**
- Modify: `frontend/locales/en-US/recording.json` (add keys)
- Modify: `frontend/locales/zh-CN/recording.json` (add keys)
- Modify: `frontend/src/components/RecordingControls.tsx`

- [ ] **Step 3.1: 扩展 recording.json 两侧**

在两个 `controls` 对象里加：
```json
"processing": "Processing recording...",
"start_tooltip": "Start recording",
"stop_tooltip": "Stop recording"
```

- [ ] **Step 3.2: RecordingControls.tsx 加 useTranslations**

```tsx
import { useTranslations } from "next-intl";
// inside RecordingControls():
const t = useTranslations("recording");
```

- [ ] **Step 3.3: 替换字符串**

| 旧 | 新 |
|---|
| `<span className="text-sm text-gray-600">Processing recording...</span>` | `<span className="text-sm text-gray-600">{t("controls.processing")}</span>` |
| `<p>Start recording</p>` | `<p>{t("controls.start_tooltip")}</p>` |
| `<p>Stop recording</p>` | `<p>{t("controls.stop_tooltip")}</p>` |

- [ ] **Step 3.4: build + commit**

```bash
pnpm test:i18n && pnpm run check:i18n && pnpm build
git add frontend/locales/en-US/recording.json frontend/locales/zh-CN/recording.json frontend/src/components/RecordingControls.tsx
git commit -m "feat(i18n): migrate RecordingControls strings (PR-15)"
```

---

## Task 4: PR-16 TranscriptPanel + Recovery

**Files:**
- Modify: `frontend/locales/en-US/transcript.json`
- Modify: `frontend/locales/zh-CN/transcript.json`
- Modify: `frontend/src/app/_components/TranscriptPanel.tsx`
- Modify: `frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx`

- [ ] **Step 4.1: 扩展 transcript.json 两侧**

在 `view` 对象里加：
```json
"copy": "Copy",
"copy_title": "Copy Transcript",
"language": "Language",
"language_title": "Language"
```

在 `recovery` 对象里加：
```json
"dialog_title": "Recover Interrupted Meetings",
"interrupted_label": "Interrupted Meetings",
"preview_label": "Preview",
"preview_empty": "No preview available",
"delete_confirm": "Delete this meeting?"
```

- [ ] **Step 4.2: TranscriptPanel.tsx 迁移**

```tsx
import { useTranslations } from "next-intl";
// inside TranscriptPanel():
const t = useTranslations("transcript");
// Replace:
title="Copy Transcript" -> title={t("view.copy_title")}
Copy -> {t("view.copy")}
title="Language" -> title={t("view.language_title")}
Language -> {t("view.language")}
```

- [ ] **Step 4.3: TranscriptRecovery.tsx 迁移**

```tsx
import { useTranslations } from "next-intl";
// inside TranscriptRecovery():
const t = useTranslations("transcript");
// Replace:
Recover Interrupted Meetings -> {t("recovery.dialog_title")}
Interrupted Meetings -> {t("recovery.interrupted_label")}
Preview -> {t("recovery.preview_label")}
```

- [ ] **Step 4.4: build + commit**

```bash
pnpm test:i18n && pnpm run check:i18n && pnpm build
git add frontend/locales/en-US/transcript.json frontend/locales/zh-CN/transcript.json frontend/src/app/_components/TranscriptPanel.tsx frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx
git commit -m "feat(i18n): migrate TranscriptPanel + recovery strings (PR-16)"
```

---

## Task 5: PR-17 SummaryPanel + StatusOverlays

**Files:**
- Modify: `frontend/locales/en-US/summary.json`
- Modify: `frontend/locales/zh-CN/summary.json`
- Modify: `frontend/src/components/MeetingDetails/SummaryPanel.tsx`
- Modify: `frontend/src/app/_components/StatusOverlays.tsx`

- [ ] **Step 5.1: 扩展 summary.json 两侧**

替换整个文件为：

**en-US/summary.json**:
```json
{
  "title": "Summary",
  "panel": {
    "title": "Meeting Summary",
    "generating": "Generating AI Summary...",
    "sections": {
      "key_points": "Key Points",
      "action_items": "Action Items",
      "decisions": "Decisions",
      "main_topics": "Main Topics",
      "full_summary": "Full Summary"
    }
  },
  "actions": {
    "generate": "Generate Summary",
    "regenerate": "Regenerate",
    "edit": "Edit Summary",
    "copy": "Copy Summary"
  },
  "status": {
    "idle": "No summary yet",
    "generating": "Generating summary...",
    "ready": "Summary ready",
    "failed": "Failed to generate summary",
    "saved_locally": "Summary language saved on this device",
    "failed_to_save_lang": "Failed to save summary language",
    "failed_to_load_lang": "Could not load saved summary language"
  },
  "empty": "Generate a summary to see key points and action items here.",
  "language": {
    "label": "Summary Language",
    "auto_translate": "Auto-translate to English"
  }
}
```

**zh-CN/summary.json**:
```json
{
  "title": "总结",
  "panel": {
    "title": "会议总结",
    "generating": "正在生成 AI 总结...",
    "sections": {
      "key_points": "关键要点",
      "action_items": "行动项",
      "decisions": "决策",
      "main_topics": "主要话题",
      "full_summary": "完整总结"
    }
  },
  "actions": {
    "generate": "生成总结",
    "regenerate": "重新生成",
    "edit": "编辑总结",
    "copy": "复制总结"
  },
  "status": {
    "idle": "暂无总结",
    "generating": "正在生成总结...",
    "ready": "总结已完成",
    "failed": "生成总结失败",
    "saved_locally": "总结语言已保存到本机",
    "failed_to_save_lang": "保存总结语言失败",
    "failed_to_load_lang": "无法加载已保存的总结语言"
  },
  "empty": "生成总结后，关键点和行动项将在此显示。",
  "language": {
    "label": "总结语言",
    "auto_translate": "自动翻译为英文"
  }
}
```

- [ ] **Step 5.2: SummaryPanel.tsx 迁移**

```tsx
import { useTranslations } from "next-intl";
// inside SummaryPanel():
const t = useTranslations("summary");
// Replace:
Generating AI Summary... -> {t("panel.generating")}
Meeting Summary -> {t("panel.title")}
Key Points -> {t("panel.sections.key_points")}
Action Items -> {t("panel.sections.action_items")}
Decisions -> {t("panel.sections.decisions")}
Main Topics -> {t("panel.sections.main_topics")}
Full Summary -> {t("panel.sections.full_summary")}

- [ ] **Step 5.3: StatusOverlays.tsx 迁移**

读取文件后按需替换；如果 StatusOverlays 不含硬编码字符串则跳过本步（需先 cat）。

- [ ] **Step 5.4: build + commit**

```bash
pnpm test:i18n && pnpm run check:i18n && pnpm build
git add frontend/locales/en-US/summary.json frontend/locales/zh-CN/summary.json frontend/src/components/MeetingDetails/SummaryPanel.tsx frontend/src/app/_components/StatusOverlays.tsx
git commit -m "feat(i18n): migrate SummaryPanel + StatusOverlays strings (PR-17)"
```

---

## Task 6: 收尾

- [ ] **Step 6.1: 确认所有改动 commit**

```bash
git status
git log --oneline feature/i18n-wave2..feature/i18n-wave3
```

- [ ] **Step 6.2: 手动验证清单（沙箱外）**

- [ ] `pnpm tauri:dev` 启动
- [ ] Settings 切到简体中文
- [ ] Sidebar nav 标签、placeholder、edit-title dialog 都变中文
- [ ] RecordingControls 状态、tooltips 变中文
- [ ] TranscriptPanel 按钮、TranscriptRecovery 对话框变中文
- [ ] SummaryPanel 章节标题、状态消息变中文
- [ ] 切回英文，所有项立刻恢复

- [ ] **Step 6.3: 推 fork + 开 PR**

```bash
git push -u fork feature/i18n-wave3
```

PR 标题：`feat(i18n): Wave 3 - migrate Sidebar/Recording/Transcript/Summary (PR-14~17)`

## Self-Review Checklist

- [x] 每个 PR 独立 commit，独立 reviewable
- [x] 复用现有 namespace（recording/transcript/summary）
- [x] 新建 sidebar namespace
- [x] `check:i18n` 自动校验双 locale key 同步
- [x] 无新依赖、无架构变更
- [x] 视觉正确性靠手动清单（沙箱外）
