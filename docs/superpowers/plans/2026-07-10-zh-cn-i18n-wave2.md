# Wave 2: UI Language Persistence + Picker (PR-12 & PR-13) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Persist the user-chosen UI language across app restarts and surface a picker in Settings.
**Architecture:** Tauri plugin-store mirror of `onboarding.rs`; popover-based picker built on existing primitives.
**Tech Stack:** Rust 2021 + tauri-plugin-store 2.4, next-intl 3, React 18, cmdk

**Reference spec:** `docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave2.md`

## Task 1: 创建分支

**Files:** 0 (git only)

- [ ] **Step 1.1: 从 feature/i18n-wave1 切出新分支**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git checkout feature/i18n-wave1
git pull --ff-only 2>nul || true   # safe if no upstream set
git checkout -b feature/i18n-wave2
```

**Acceptance:** `git status` clean, on branch `feature/i18n-wave2`.

---

## Task 2: Rust i18n 模块 + 单元测试

**Files:**
- Create: `frontend/src-tauri/src/i18n.rs`
- Modify: `frontend/src-tauri/src/lib.rs` (add `pub mod i18n;` near other modules)

- [ ] **Step 2.1: 写 i18n.rs**

创建 `frontend/src-tauri/src/i18n.rs`：

```rust
use serde::{Deserialize, Serialize};
use tauri::{AppHandle, Runtime};
use tauri_plugin_store::StoreExt;
use log::{info, warn};
use anyhow::Result;

pub const SUPPORTED_LOCALES: &[&str] = &["en-US", "zh-CN"];
pub const DEFAULT_LOCALE: &str = "en-US";
const STORE_FILE: &str = "ui-locale.json";
const STORE_KEY: &str = "language";

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct UiLocale {
    pub language: String,
    pub last_updated: String,
}

pub fn is_supported_locale(value: &str) -> bool {
    SUPPORTED_LOCALES.contains(&value)
}

pub async fn load_ui_language<R: Runtime>(
    app: &AppHandle<R>,
) -> Result<Option<UiLocale>> {
    let store = match app.store(STORE_FILE) {
        Ok(s) => s,
        Err(e) => {
            warn!("Failed to access ui-locale store: {}, returning None", e);
            return Ok(None);
        }
    };
    match store.get(STORE_KEY) {
        Some(v) => match serde_json::from_value::<UiLocale>(v.clone()) {
            Ok(loc) => Ok(Some(loc)),
            Err(e) => {
                warn!("Failed to deserialize ui-locale entry: {}", e);
                Ok(None)
            }
        },
        None => Ok(None),
    }
}

pub async fn save_ui_language<R: Runtime>(
    app: &AppHandle<R>,
    language: &str,
) -> Result<()> {
    if !is_supported_locale(language) {
        return Err(anyhow::anyhow!("Unsupported locale: {}", language));
    }
    let store = app.store(STORE_FILE)
        .map_err(|e| anyhow::anyhow!("Failed to access ui-locale store: {}", e))?;
    let payload = UiLocale {
        language: language.to_string(),
        last_updated: chrono::Utc::now().to_rfc3339(),
    };
    let value = serde_json::to_value(&payload)?;
    store.set(STORE_KEY, value);
    store.save().map_err(|e| anyhow::anyhow!("Failed to persist ui-locale: {}", e))?;
    info!("Saved UI locale: {}", language);
    Ok(())
}

pub async fn reset_ui_language<R: Runtime>(app: &AppHandle<R>) -> Result<()> {
    let store = app.store(STORE_FILE)
        .map_err(|e| anyhow::anyhow!("Failed to access ui-locale store: {}", e))?;
    store.delete(STORE_KEY);
    store.save().map_err(|e| anyhow::anyhow!("Failed to persist ui-locale reset: {}", e))?;
    info!("Reset UI locale");
    Ok(())
}

#[tauri::command]
pub async fn get_ui_language<R: Runtime>(
    app: AppHandle<R>,
) -> Result<Option<String>, String> {
    load_ui_language(&app)
        .await
        .map(|opt| opt.map(|l| l.language))
        .map_err(|e| format!("Failed to load ui language: {}", e))
}

#[tauri::command]
pub async fn set_ui_language<R: Runtime>(
    app: AppHandle<R>,
    language: String,
) -> Result<(), String> {
    if !is_supported_locale(&language) {
        return Err(format!("Unsupported locale: {}", language));
    }
    save_ui_language(&app, &language)
        .await
        .map_err(|e| format!("Failed to save ui language: {}", e))
}

#[tauri::command]
pub async fn reset_ui_language_cmd<R: Runtime>(
    app: AppHandle<R>,
) -> Result<(), String> {
    reset_ui_language(&app)
        .await
        .map_err(|e| format!("Failed to reset ui language: {}", e))
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn supported_locales_match_frontend() {
        // Mirror of frontend/src/i18n/config.ts LOCALES constant.
        // Cross-checked at PR review time.
        assert_eq!(SUPPORTED_LOCALES, &["en-US", "zh-CN"]);
    }

    #[test]
    fn rejects_unsupported_locale() {
        assert!(!is_supported_locale("fr-FR"));
        assert!(!is_supported_locale(""));
        assert!(!is_supported_locale("EN-us"));
        assert!(is_supported_locale("en-US"));
        assert!(is_supported_locale("zh-CN"));
    }

    #[test]
    fn ui_locale_roundtrips() {
        let loc = UiLocale {
            language: "zh-CN".to_string(),
            last_updated: "2026-07-10T00:00:00Z".to_string(),
        };
        let v = serde_json::to_value(&loc).unwrap();
        let back: UiLocale = serde_json::from_value(v).unwrap();
        assert_eq!(back.language, "zh-CN");
        assert_eq!(back.last_updated, "2026-07-10T00:00:00Z");
    }

    #[test]
    fn backward_compat_missing_last_updated() {
        // Older Wave 1 stored shape only had `language`. Tolerate.
        let v: serde_json::Value = serde_json::json!({"language": "en-US"});
        let loc: UiLocale = serde_json::from_value(v).unwrap();
        assert_eq!(loc.language, "en-US");
    }
}
```

- [ ] **Step 2.2: 在 lib.rs 注册模块**

修改 `frontend/src-tauri/src/lib.rs`，在 `pub mod onboarding;` 后面加：

```rust
pub mod i18n;
```

- [ ] **Step 2.3: 注册 Tauri 命令**

在 `lib.rs` 的 `invoke_handler` builder 里，紧跟 onboarding 命令后面加：

```rust
            i18n::get_ui_language,
            i18n::set_ui_language,
            i18n::reset_ui_language_cmd,
```

- [ ] **Step 2.4: cargo check**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily\frontend\src-tauri
cargo check --lib 2>&1 | tail -30
```

**Acceptance:** `cargo check` 通过；4 个新单元测试在 `cargo test --lib i18n` 下通过。

**注意：** 全量 `cargo build` 需要 whisper-rs / ffmpeg / onnx 等 native 依赖，沙箱跑不动；`cargo check --lib` 即可覆盖本任务涉及的代码。

- [ ] **Step 2.5: Commit**

```bash
git add frontend/src-tauri/src/i18n.rs frontend/src-tauri/src/lib.rs
git commit -m "feat(i18n): add get/set_ui_language Tauri commands (PR-12)"
```

---

## Task 3: 前端清理 fallback swallows

**Files:**
- Modify: `frontend/src/i18n/request.ts`
- Modify: `frontend/src/hooks/useLocale.tsx`

- [ ] **Step 3.1: request.ts 的 getStoredLocale 移除 try/catch**

当前代码：

```ts
export async function getStoredLocale(): Promise<Locale> {
  try {
    const { invoke } = await import("@tauri-apps/api/core");
    const stored = await invoke<string>("get_ui_language");
    if (isSupportedLocale(stored)) return stored;
  } catch { /* fallback */ }
  return DEFAULT_LOCALE;
}
```

改为：

```ts
export async function getStoredLocale(): Promise<Locale> {
  const { invoke } = await import("@tauri-apps/api/core");
  const stored = await invoke<string | null>("get_ui_language");
  if (stored && isSupportedLocale(stored)) return stored;
  return DEFAULT_LOCALE;
}
```

**Note:** 现在 `get_ui_language` 已注册，错误会真实反映问题（store 文件权限、IO 错误等）；不需要静默吞掉。Null（首次启动无存储）走默认 locale。

- [ ] **Step 3.2: useLocale.tsx 的 setLocale 移除 try/catch**

当前代码：

```ts
try {
  const { invoke } = await import("@tauri-apps/api/core");
  await invoke("set_ui_language", { language: next });
} catch {
  // PR-12 not yet merged - accept the change in-memory only.
}
setLocaleState(next);
```

改为：先持久化，成功后再更新内存态：

```ts
const { invoke } = await import("@tauri-apps/api/core");
await invoke("set_ui_language", { language: next });
setLocaleState(next);
```

注意：如果 set 失败，React 状态不变，错误向上抛给 UI 调用方（picker 自己 try/catch 显示 toast）。

- [ ] **Step 3.3: 跑测试**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily\frontend
pnpm test:i18n
```

**Acceptance:** 9/9 tests 仍然通过。

- [ ] **Step 3.4: 跑 build**

```bash
pnpm build 2>&1 | tail -15
```

**Acceptance:** build 通过，无新增类型错误。

- [ ] **Step 3.5: Commit**

```bash
git add frontend/src/i18n/request.ts frontend/src/hooks/useLocale.tsx
git commit -m "refactor(i18n): remove silent fallbacks now that Tauri commands exist"
```

---

## Task 4: LanguagePickerPopover 组件

**Files:**
- Create: `frontend/src/components/LanguagePickerPopover.tsx`
- Create: `frontend/tests/i18n/language-picker.test.tsx`

- [ ] **Step 4.1: 写组件**

创建 `frontend/src/components/LanguagePickerPopover.tsx`：

```tsx
"use client";

import { useState } from "react";
import { Check, ChevronsUpDown, Languages } from "lucide-react";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { Command, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList } from "@/components/ui/command";
import { Button } from "@/components/ui/button";
import { toast } from "sonner";
import { useLocale } from "@/hooks/useLocale";
import { LOCALES, type Locale } from "@/i18n/config";
import { useTranslations } from "next-intl";

export function LanguagePickerPopover() {
  const t = useTranslations("settings");
  const { locale, setLocale } = useLocale();
  const [open, setOpen] = useState(false);
  const [pending, setPending] = useState(false);

  const handleSelect = async (next: Locale) => {
    if (next === locale) { setOpen(false); return; }
    setPending(true);
    try {
      await setLocale(next);
      setOpen(false);
    } catch (e) {
      toast.error(t("preference.locale_change_failed"), {
        description: e instanceof Error ? e.message : String(e),
      });
    } finally {
      setPending(false);
    }
  };

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button variant="outline" role="combobox" aria-expanded={open} className="w-[220px] justify-between">
          <span className="flex items-center gap-2">
            <Languages className="h-4 w-4" />
            {t(`preference.locale_name_${locale.toLowerCase().replace("-", "_")}`)}
          </span>
          <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-[240px] p-0" align="start">
        <Command>
          <CommandInput placeholder={t("preference.locale_search")} />
          <CommandList>
            <CommandEmpty>{t("preference.locale_empty")}</CommandEmpty>
            <CommandGroup>
              {LOCALES.map((l) => (
                <CommandItem
                  key={l}
                  value={l}
                  disabled={pending}
                  onSelect={() => handleSelect(l as Locale)}
                >
                  {t(`preference.locale_name_${l.toLowerCase().replace("-", "_")}`)}
                  <Check className={`ml-auto h-4 w-4 ${l === locale ? "opacity-100" : "opacity-0"}`} />
                </CommandItem>
              ))}
            </CommandGroup>
          </CommandList>
        </Command>
      </PopoverContent>
    </Popover>
  );
}
```

- [ ] **Step 4.2: 添加 locale 展示名 + 错误 toast key**

在 `frontend/locales/en-US/settings.json` 加：

```json
"locale_name_en_us": "English (US)",
"locale_name_zh_cn": "Simplified Chinese",
"locale_search": "Search languages...",
"locale_empty": "No matching language",
"locale_change_failed": "Failed to switch language"
```

放在 `preference` 对象内。

在 `frontend/locales/zh-CN/settings.json` 加：

```json
"locale_name_en_us": "English (US)",
"locale_name_zh_cn": "简体中文",
"locale_search": "搜索语言...",
"locale_empty": "没有匹配的语言",
"locale_change_failed": "切换语言失败"
```

- [ ] **Step 4.3: 跑 check:i18n**

```bash
pnpm run check:i18n
```

**Acceptance:** "i18n check passed"

- [ ] **Step 4.4: 写单元测试**

创建 `frontend/tests/i18n/language-picker.test.tsx`：

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { cleanup, render, screen, fireEvent, waitFor } from "@testing-library/react";
import { NextIntlClientProvider } from "next-intl";
import { LocaleProvider } from "@/hooks/useLocale";
import { LanguagePickerPopover } from "@/components/LanguagePickerPopover";
import enMessages from "@/../locales/en-US/settings.json";

vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn() }));
import { invoke } from "@tauri-apps/api/core";
const mockInvoke = vi.mocked(invoke);

function renderPicker() {
  return render(
    <NextIntlClientProvider locale="en-US" messages={{ settings: enMessages }}>
      <LocaleProvider initial="en-US">
        <LanguagePickerPopover />
      </LocaleProvider>
    </NextIntlClientProvider>
  );
}

describe("LanguagePickerPopover", () => {
  beforeEach(() => {
    mockInvoke.mockReset();
    mockInvoke.mockResolvedValue(null);
  });
  afterEach(() => { cleanup(); vi.clearAllMocks(); });

  it("shows current locale name as trigger label", () => {
    renderPicker();
    expect(screen.getByRole("combobox")).toHaveTextContent("English (US)");
  });

  it("opens popover and lists all supported locales", async () => {
    renderPicker();
    fireEvent.click(screen.getByRole("combobox"));
    await waitFor(() => {
      expect(screen.getByText("Simplified Chinese")).toBeInTheDocument();
    });
  });

  it("calls set_ui_language on selection", async () => {
    renderPicker();
    mockInvoke.mockResolvedValueOnce(undefined);
    fireEvent.click(screen.getByRole("combobox"));
    fireEvent.click(screen.getByText("Simplified Chinese"));
    await waitFor(() => {
      expect(mockInvoke).toHaveBeenCalledWith("set_ui_language", { language: "zh-CN" });
    });
  });
});
```

- [ ] **Step 4.5: 跑测试**

```bash
pnpm test:i18n
```

**Acceptance:** 12/12 tests pass (3 new picker + 9 existing).

- [ ] **Step 4.6: Commit**

```bash
git add frontend/src/components/LanguagePickerPopover.tsx frontend/tests/i18n/language-picker.test.tsx frontend/locales/
git commit -m "feat(i18n): add LanguagePickerPopover component (PR-13 part 1)"
```

---

## Task 5: 设置页接入 picker

**Files:**
- Modify: `frontend/src/components/PreferenceSettings.tsx`

- [ ] **Step 5.1: 在 PreferenceSettings 顶部插入 picker**

在 `PreferenceSettings.tsx` 的 import 块加：

```tsx
import { LanguagePickerPopover } from "./LanguagePickerPopover";
import { useTranslations } from "next-intl";
```

把组件顶部的 `export function PreferenceSettings() {` 后加：

```tsx
  const t = useTranslations("settings");
```

在 `<div className="space-y-6">` 内最前面（Notifications card 之前）插入：

```tsx
      {/* Interface Language */}
      <div className="bg-white rounded-lg border border-gray-200 p-6 shadow-sm">
        <div className="flex items-center justify-between">
          <div>
            <h3 className="text-lg font-semibold text-gray-900 mb-2">{t("preference.title")}</h3>
            <p className="text-sm text-gray-600">{t("preference.language")}</p>
          </div>
          <LanguagePickerPopover />
        </div>
      </div>
```

- [ ] **Step 5.2: build**

```bash
pnpm build 2>&1 | tail -15
```

**Acceptance:** build 通过，0 类型错误。

- [ ] **Step 5.3: 完整测试**

```bash
pnpm test:i18n
pnpm run check:i18n
pnpm build
```

**Acceptance:** 三项全部通过。

- [ ] **Step 5.4: Commit**

```bash
git add frontend/src/components/PreferenceSettings.tsx
git commit -m "feat(i18n): mount LanguagePickerPopover in Settings > General"
```

---

## Task 6: 收尾

**Files:** 0 (git only)

- [ ] **Step 6.1: 确认所有改动已 commit**

```bash
git status
```

**Acceptance:** working tree clean.

- [ ] **Step 6.2: 显示本 wave 的 commit 列表**

```bash
git log --oneline feature/i18n-wave1..feature/i18n-wave2
```

**Acceptance:** 至少 4 条 commit（PR-12 + PR-13）。

- [ ] **Step 6.3: 手动验证清单（沙箱外执行）**

清单（不写代码，留给用户）：
- [ ] `pnpm tauri:dev` 启动应用
- [ ] 进 Settings → General，看到 "Interface Language" 卡片 + picker
- [ ] 切到简体中文，所有标题/标签立刻变中文
- [ ] 退出应用，重新 `pnpm tauri:dev`
- [ ] 启动后默认仍是简体中文（持久化生效）
- [ ] 切回英文，重复上述持久化验证
- [ ] 在 picker 里选不存在的 locale（直接 `invoke`）应该返回错误（这一项可跳过）

- [ ] **Step 6.4: 推送到 fork 并开 PR**

```bash
git push -u fork feature/i18n-wave2
```

PR 标题：`feat(i18n): Wave 2 - UI language persistence + picker (PR-12, PR-13)`

---

## Self-Review Checklist

- [x] Persistence 镜像 `onboarding.rs`（不引入新模式）
- [x] 写时在 Rust 侧显式校验 locale 白名单
- [x] 前端 try/catch 静默吞错已移除
- [x] Picker 仅用现有 UI 原语（Popover + cmdk + Button + lucide 图标）
- [x] Locale 显示名本身也是可本地化 key
- [x] 每一层都有测试：Rust serde 往返 + 前端 picker 组件
- [x] 持久化往返通过手动清单验证（沙箱外）
