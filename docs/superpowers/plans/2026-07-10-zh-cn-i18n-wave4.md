# Wave 4: 残留 polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans.

**Goal:** 完成 Wave 1~3 后剩余 ~47 条英文硬编码字符串的本地化。
**Architecture:** 机械 `useTranslations()` 替换；不引入新依赖；扩展现有 namespace。
**Tech Stack:** next-intl 4 (Wave 1 已装)

**Reference spec:** `docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave4.md`

## Task 1: 创建分支 + 提交 spec/plan

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git checkout feature/i18n-wave3
git checkout -b feature/i18n-wave4
git add docs/superpowers/specs/2026-07-10-zh-cn-i18n-wave4.md docs/superpowers/plans/2026-07-10-zh-cn-i18n-wave4.md
git commit -m "docs(i18n): add Wave 4 spec + implementation plan (PR-18~21)"
```

---

## Task 2: PR-18 RecordingControls remaining

**Files:**
- Modify: `frontend/locales/en-US/recording.json`
- Modify: `frontend/locales/zh-CN/recording.json`
- Modify: `frontend/src/components/RecordingControls.tsx`

**Keys to add (recording.json):**
```json
"status": {
  ...
  "pausing": "Pausing...",
  "resuming": "Resuming...",
  "stopping": "Stopping...",
  "validating": "Validating speech recognition...",
  "pause_tooltip": "Pause recording",
  "resume_tooltip": "Resume recording"
},
"errors": {
  "init_failed": "Failed to initialize recording. Please check the console for details.",
  "pause_failed": "Failed to pause recording. Please check the console for details.",
  "resume_failed": "Failed to resume recording. Please check the console for details.",
  "close_alert": "Close alert",
  "device_mic_title": "Microphone Not Available",
  "device_mic_message": "Unable to access your microphone. Please check that:\n• Your microphone is connected\n• The app has microphone permissions\n• No other app is using the microphone",
  "device_speaker_title": "System Audio Not Available",
  "device_speaker_message": "Unable to capture system audio. Please check that:\n• A virtual audio device (like BlackHole) is installed\n• The app has screen recording permissions (macOS)\n• System audio is properly configured",
  "device_permission_title": "Permission Required",
  "device_permission_message": "Recording permissions are required. Please:\n• Grant microphone access in System Settings\n• Grant screen recording access for system audio (macOS)\n• Restart the app after granting permissions",
  "device_generic_title": "Recording Failed",
  "device_generic_message": "Unable to start recording. Please check your audio device settings and try again."
}
```

**Component edits (RecordingControls.tsx):**
- Reuse existing `useTranslations("recording")` hook
- L82: `alert('Failed to initialize...')` → `alert(t("errors.init_failed"))`
- L120-121, 125-126, 130-131, 135-136: each device error title/message pair → `t("errors.device_X_title")` / `t("errors.device_X_message")`
- L218, L236: `alert('Failed to pause/resume...')` → `alert(t("errors.pause_failed"))` / `alert(t("errors.resume_failed"))`
- L440: `{isPausing ? '' : ''}` → `{isPausing ? t("status.pausing") : t("status.resuming")}`
- L446: `{isPaused ? '' : ''}` → `{isPaused ? t("status.resume_tooltip") : t("status.pause_tooltip")}`
- L464: `Stopping...` → `{t("status.stopping")}`
- L498: `Validating speech recognition...` → `{t("status.validating")}`
- L509: `aria-label="Close alert"` → `aria-label={t("errors.close_alert")}`

**Acceptance:** build 通过；目视 zh-CN 下 RecordingControls 全部本地化。

**Verification + commit:**
```bash
cd frontend
pnpm test:i18n && pnpm run check:i18n && pnpm build
git add frontend/locales/en-US/recording.json frontend/locales/zh-CN/recording.json frontend/src/components/RecordingControls.tsx
git commit -m "feat(i18n): migrate RecordingControls remaining strings (PR-18)"
```

---

## Task 3: PR-19 TranscriptRecovery remaining

**Files:**
- Modify: `frontend/locales/en-US/transcript.json`
- Modify: `frontend/locales/zh-CN/transcript.json`
- Modify: `frontend/src/components/TranscriptRecovery/TranscriptRecovery.tsx`

**Keys to add (transcript.json):**
```json
"recovery": {
  ...
  "dialog_description": "We found {count} meeting{plural} that {verb} interrupted. Select a meeting to preview and recover it.",
  "started_label": "Started {date}",
  "preview_loading": "Loading preview...",
  "preview_empty": "No transcripts to preview",
  "preview_select": "Select a meeting to preview",
  "audio_available": "Audio available",
  "audio_unavailable": "No audio",
  "alert_recover_failed": "Failed to recover meeting. Please try again.",
  "alert_delete_failed": "Failed to delete meeting. Please try again.",
  "confirm_delete": "Are you sure you want to delete this meeting? This cannot be undone.",
  "cancel": "Cancel",
  "delete": "Delete",
  "deleting": "Deleting...",
  "recover": "Recover",
  "recovering": "Recovering..."
}
```

(注：`{count}/{plural}/{verb}` 用 ICU / next-intl 的占位语法需 plural rule。简化：保留英文 fallback 处理 `{n}` 单复数，本轮只迁静态文本，把描述字符串整段翻译而不是占位。)

**Component edits (TranscriptRecovery.tsx):**
- Reuse existing `useTranslations("transcript")` hook
- L92, L101, L112: 3 个 alert/confirm → `t("recovery.alert_recover_failed")` / `t("recovery.confirm_delete")` / `t("recovery.alert_delete_failed")`
- L126: DialogDescription 整段替换为预格式化的中文（在 component 内根据 count 拼接单复数）→ 用 `t.rich` 或拆 2 key（`dialog_description_one`, `dialog_description_other`）。**Scope 限制**: 把 DialogDescription 翻译为单一中文文本"恢复未完成的会议"作为主标题描述，复杂单复数暂保留英文 fallback；或使用 next-intl 的 `t.rich` 处理。
  - 简化决策：直接整句中文化 `我们检测到 {n} 个会议被中断，请选择预览并恢复。` 用 next-intl placeholders 或简单 template string `t("recovery.dialog_description", { count })` (next-intl 支持 placeholder via second arg)
- L160, L164: `title="Audio available"` / `title="No audio"` → `title={t("recovery.audio_available")}` / `title={t("recovery.audio_unavailable")}`
- L185: `Started ` 前缀 → 用 `t.rich` 或新 key `recovery.started_label`
- L210: `Loading preview...` → `{t("recovery.preview_loading")}`
- L256: `No transcripts to preview` → `{t("recovery.preview_empty")}`
- L263: `Select a meeting to preview` → `{t("recovery.preview_select")}`
- L276: `Cancel` → `{t("recovery.cancel")}`
- L286: `Deleting...` → `{t("recovery.deleting")}`
- L291: `Delete` → `{t("recovery.delete")}`
- L302: `Recovering...` → `{t("recovery.recovering")}`
- L307: `Recover` → `{t("recovery.recover")}`

**plural 处理策略**：复用 Wave 3 的简单模式，json 写单一 string（zh-CN 无单复数），en-US 写带 placeholder 语法。Component 端不再写 JSX 内的 ternary 单复数，改调用 `t("recovery.dialog_description", { count })`，再渲染结果。next-intl v4 placeholder 用 `{count}` 替换 (e.g., `"We found {count, plural, one {# meeting} other {# meetings}} that were interrupted..."`)。

**Acceptance:** zh-CN 下 Recovery 整 dialog 本地化；en-US 维持原 behavior。

**Verify + commit:** 同 Task 2 模板。

---

## Task 4: PR-20 SummaryPanel + sibling toasts

**Files:**
- Modify: `frontend/locales/en-US/summary.json`
- Modify: `frontend/locales/zh-CN/summary.json`
- Modify: `frontend/src/components/MeetingDetails/SummaryPanel.tsx`
- Modify: `frontend/src/components/MeetingDetails/SummaryGeneratorButtonGroup.tsx` (新加 useTranslations)
- Modify: `frontend/src/components/MeetingDetails/RetranscribeDialog.tsx`
- Modify: `frontend/src/components/MeetingDetails/TranscriptPanel.tsx`

**Keys (summary.json):**
```json
"panel": {
  ...
  "auto_language_subtitle": "Uses dominant transcript language",
  "saved_locally_subtitle": "Saved on this device for folderless meetings",
  "summary_language_tooltip": "Summary language: {label}",
  "summary_language_tooltip_fallback": "Summary language: {label} (saved on this device)",
  "set_summary_language": "Set summary language"
},
"status": {
  ...
  "load_failed_description": "Using Auto until meeting metadata can be read.",
  "saved_locally_description": "This meeting has no recording folder, so the preference cannot be written to meeting metadata."
},
"custom_prompt_placeholder": "Add context for AI summary. For example people involved, meeting overview, objective etc..."
```

(For 多个 model error toasts under `summary.errors.*`)

**Component edits:**
- SummaryPanel L124-125: 2 个 subtitle → `t("panel.saved_locally_subtitle")` / `t("panel.auto_language_subtitle")`
- SummaryPanel L142 (toast description): → `t("status.load_failed_description")`
- SummaryPanel L175 (toast description): → `t("status.saved_locally_description")`
- SummaryPanel L235: title template string → `t.rich("panel.summary_language_tooltip", { label: ... })` 或拆 2 key
- SummaryPanel L236: `aria-label="Set summary language"` → `aria-label={t("panel.set_summary_language")}`
- MeetingDetails/TranscriptPanel L104: placeholder → `placeholder={t("custom_prompt_placeholder")}` (需在该 component 加 useTranslations hook)
- SummaryGeneratorButtonGroup: 9 个 toast message → `t("errors.model_X")` 等新 key（9 个 keys under `summary.errors.*`）
- RetranscribeDialog L156 / L239: 2 个 toast message → 同 namespace 下扩展

**简化决策**：为了让 PR 不过大，PR-20 只做 SummaryPanel + MeetingDetails/TranscriptPanel placeholder；SummaryGeneratorButtonGroup 9 个 + RetranscribeDialog 2 个 toasts 独立为 **PR-20b: model error toasts** (也可并入 PR-20 但拆分 review 更干净)。

**Verify + commit:** 标准循环。

---

## Task 5: PR-21 Sidebar a11y polish + small sweep

**Files:**
- Modify: `frontend/locales/en-US/sidebar.json`
- Modify: `frontend/locales/zh-CN/sidebar.json`
- Modify: `frontend/src/components/Sidebar/index.tsx`

**Keys (sidebar.json):**
```json
"meeting_list": {
  ...
  "edit_title_aria": "Edit meeting title",
  "delete_aria": "Delete meeting"
}
```

**Component edits (Sidebar/index.tsx L629, L639):**
- 2 处 aria-label 替换
- `useTranslations("sidebar")` 已存在

**PR-21 也可附加**：sweep 其他高可见小遗漏（如 PermissionWarning、Notes pages）。**scope 决策**：严格按 spec 只做 Sidebar 2 处，留其他给后续 wave。

---

## Task 6: 收尾

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git status
git log --oneline feature/i18n-wave3..feature/i18n-wave4
```

期望：5 commits (1 docs + 4 PR)。

**沙箱外人工验证清单：**
- `pnpm tauri:dev` 启动
- 切到简体中文，逐组件目视：
  - RecordingControls：按下 pause，看到 "正在暂停..."；打开 device-error 弹框看 4 套中文
  - TranscriptRecovery：触发 recovery dialog，预览区/按钮/标题全中文
  - SummaryPanel：触发 load/save language toast，看 description 中文；按钮 aria 中文
  - SummaryGeneratorButtonGroup：触发任意 model error toast 全中文
  - Sidebar：hover "Edit meeting title" / "Delete meeting" icon 看 tooltip 中文
- 切回 en-US 全部恢复
- `git push -u fork feature/i18n-wave4` + 开 PR（sandbox 无法做）

## Self-Review Checklist

- [ ] 不新增 namespace
- [ ] 每 PR 一个 commit
- [ ] check:i18n + test:i18n + build 全过
- [ ] 修 PR-14-like 漏注册 (本轮无新增 namespace，理论上不会复发但保留警觉)