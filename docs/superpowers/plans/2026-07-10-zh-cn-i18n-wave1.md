# Wave 1: Meetily zh-CN i18n Infrastructure (PR-11) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 next-intl 基础设施——6 个 locale JSON baseline、20 词术语表、CI 校验脚本——支持后续 Wave 2~4 的 i18n 改造。

**Architecture:** 在 `frontend/` 下建立 i18n 分层：
- `frontend/locales/{en-US,zh-CN}/*.json`（6 namespace × 2 语言 = 12 个文件）
- `frontend/src/i18n/`（config.ts、request.ts 模块）
- `frontend/src/hooks/useLocale.ts`（即时切换 hook）
- `frontend/scripts/check-i18n.ts`（CI 校验）
- `docs/i18n/glossary.md`（术语表）
- `app/layout.tsx` 包 `NextIntlClientProvider` + `LocaleProvider`

**Tech Stack:** next-intl 3.x · TypeScript 5.x · Next.js 14 App Router · pnpm 9.x · tsx 4.x（脚本运行） · vitest（单元测试，可选）

**参考 Spec:** `docs/superpowers/specs/2026-07-10-zh-cn-i18n-design.md` §3-§7

**前置依赖：**
- Node.js ≥ 18.17
- pnpm ≥ 9
- 仓库根目录：`C:\Users\qjl10\Documents\工作区\meetily`
- 当前在 feature/i18n-design-spec 分支（或基于 main 新建 feature/i18n-wave1 分支）

---

## File Structure

**新建文件**：
- `frontend/locales/en-US/common.json`
- `frontend/locales/en-US/recording.json`
- `frontend/locales/en-US/transcript.json`
- `frontend/locales/en-US/summary.json`
- `frontend/locales/en-US/settings.json`
- `frontend/locales/en-US/errors.json`
- `frontend/locales/zh-CN/common.json`
- `frontend/locales/zh-CN/recording.json`
- `frontend/locales/zh-CN/transcript.json`
- `frontend/locales/zh-CN/summary.json`
- `frontend/locales/zh-CN/settings.json`
- `frontend/locales/zh-CN/errors.json`
- `frontend/src/i18n/config.ts`
- `frontend/src/i18n/request.ts`
- `frontend/src/hooks/useLocale.ts`
- `frontend/scripts/check-i18n.ts`
- `frontend/tests/i18n/check-i18n.test.ts`
- `frontend/tests/i18n/load-messages.test.ts`
- `frontend/tests/i18n/glossary-format.test.ts`
- `docs/i18n/glossary.md`
- `docs/i18n/README.md`

**修改文件**：
- `frontend/package.json`（加 next-intl 依赖 + `check:i18n` script）
- `frontend/next.config.js`（加 next-intl plugin）
- `frontend/src/app/layout.tsx`（包 NextIntlClientProvider + LocaleProvider）
- `frontend/src/app/page.tsx`（样例字符串迁移）
- `.github/workflows/ci.yml`（加 check:i18n step）

---
### Task 1: 验证环境 + 切到新分支

**Files:** 无（环境操作）

- [ ] **Step 1.1: 验证 Node.js / pnpm 版本**

```bash
node --version    # 应 ≥ v18.17
pnpm --version    # 应 ≥ 9.0
```

如果版本低，访问 https://nodejs.org/ 与 https://pnpm.io/installation 安装。

- [ ] **Step 1.2: 切到工作分支**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git checkout main
git pull
git checkout -b feature/i18n-wave1
```

- [ ] **Step 1.3: 安装依赖（验证基线可编译）**

```bash
cd frontend
pnpm install
```

预期：依赖安装完成无错误。

- [ ] **Step 1.4: 提交基线验证（无文件改动应无 commit）**

```bash
cd ..
git status
```

预期：工作区干净。如果有未提交的改动（来自 spec 之前的工作），stash 或 commit 它们。

---

### Task 2: 添加 next-intl 依赖

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 2.1: 装 next-intl**

```bash
cd frontend
pnpm add next-intl
```

预期：`next-intl@^3.x.x` 加入 `dependencies`。具体版本以装到的为准（v3.22+ 适配 Next 14 App Router）。

- [ ] **Step 2.2: 装 tsx（用于 check:i18n 脚本）**

```bash
pnpm add -D tsx
```

预期：`tsx@^4.x.x` 加入 `devDependencies`。

- [ ] **Step 2.3: 添加 npm scripts**

修改 `frontend/package.json` 的 `scripts` 段：

```json
{
  "scripts": {
    "check:i18n": "tsx scripts/check-i18n.ts",
    "test:i18n": "vitest run tests/i18n"
  }
}
```

如果 `scripts` 已有 `test` script，合并而非覆盖。完整 scripts 段示例：

```json
{
  "scripts": {
    "dev": "next dev -p 3118",
    "build": "next build",
    "start": "next start -p 3118",
    "lint": "next lint",
    "check:i18n": "tsx scripts/check-i18n.ts",
    "test:i18n": "vitest run tests/i18n"
  }
}
```

- [ ] **Step 2.4: 装 vitest（单元测试）**

```bash
pnpm add -D vitest @vitest/ui
```

- [ ] **Step 2.5: 验证 pnpm install 干净**

```bash
pnpm install
```

预期：无错误。

- [ ] **Step 2.6: Commit**

```bash
git add frontend/package.json frontend/pnpm-lock.yaml
git commit -m "chore(deps): add next-intl, tsx, vitest for i18n infrastructure"
```

---

### Task 3: 配置 next-intl plugin

**Files:**
- Modify: `frontend/next.config.js`

- [ ] **Step 3.1: 查看现有 next.config.js**

```bash
cat frontend/next.config.js
```

预期：现有配置可能包含 `output: 'export'`（Tauri 静态构建）。记录现有设置。

- [ ] **Step 3.2: 包装现有 config 加 createNextIntlPlugin**

修改 `frontend/next.config.js`：

```js
const createNextIntlPlugin = require('next-intl/plugin');

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 保留现有配置（output: 'export' 等）
};

module.exports = withNextIntl(nextConfig);
```

如果原 config 是 ESM（`export default`），改为：

```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 保留现有配置
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 3.3: 验证 next build 仍能运行**

```bash
cd frontend
pnpm build 2>&1 | head -30
```

预期：构建报告"i18n/request.ts not found"或类似错误（因为 Task 4 才创建该文件），但 next-intl plugin 已正确加载。错误是预期的，下个 task 解决。

- [ ] **Step 3.4: Commit**

```bash
git add frontend/next.config.js
git commit -m "chore(next): wrap config with next-intl plugin"
```

---

### Task 4: 创建 i18n 核心模块（config + request + loadMessages）

**Files:**
- Create: `frontend/src/i18n/config.ts`
- Create: `frontend/src/i18n/request.ts`
- Create: `frontend/tests/i18n/load-messages.test.ts`

- [ ] **Step 4.1: 写 config.ts**

创建 `frontend/src/i18n/config.ts`：

```typescript
// Supported locales for the Meetily desktop app.
// Add new locales here when extending language support.
export const LOCALES = ['en-US', 'zh-CN'] as const;
export type Locale = (typeof LOCALES)[number];

export const DEFAULT_LOCALE: Locale = 'en-US';

// Map OS locale (e.g. 'zh-CN', 'zh-TW', 'en-US') to a supported locale.
// Returns DEFAULT_LOCALE if no match.
export function resolveLocaleFromOs(osLocale: string | null | undefined): Locale {
  if (!osLocale) return DEFAULT_LOCALE;
  const lower = osLocale.toLowerCase();
  if (lower.startsWith('zh')) return 'zh-CN';
  if (lower.startsWith('en')) return 'en-US';
  return DEFAULT_LOCALE;
}

export function isSupportedLocale(value: string): value is Locale {
  return (LOCALES as readonly string[]).includes(value);
}
```

- [ ] **Step 4.2: 写 request.ts**

创建 `frontend/src/i18n/request.ts`：

```typescript
import { getRequestConfig } from 'next-intl/server';
import { invoke } from '@tauri-apps/api/core';
import { DEFAULT_LOCALE, isSupportedLocale, type Locale } from './config';

/**
 * Read the user's stored UI language from the Tauri settings table.
 * Falls back to DEFAULT_LOCALE if the value is missing or invalid.
 * This function is safe to call during SSR — it is also called from the
 * client (LocaleProvider) where `invoke` works normally.
 */
export async function getStoredLocale(): Promise<Locale> {
  try {
    const stored = await invoke<string>('get_ui_language');
    if (isSupportedLocale(stored)) return stored;
  } catch {
    // Tauri command not available (e.g. plain `next dev` without Tauri shell).
    // PR-19 will add OS locale fallback. For now, return default.
  }
  return DEFAULT_LOCALE;
}

/**
 * Load all message namespaces for a given locale. Returns an object keyed by
 * namespace name, suitable for `messages` in the next-intl request config.
 */
export async function loadMessages(locale: Locale) {
  const [common, recording, transcript, summary, settings, errors] = await Promise.all([
    import(`../../locales/${locale}/common.json`),
    import(`../../locales/${locale}/recording.json`),
    import(`../../locales/${locale}/transcript.json`),
    import(`../../locales/${locale}/summary.json`),
    import(`../../locales/${locale}/settings.json`),
    import(`../../locales/${locale}/errors.json`),
  ]);
  return {
    common: common.default,
    recording: recording.default,
    transcript: transcript.default,
    summary: summary.default,
    settings: settings.default,
    errors: errors.default,
  };
}

export default getRequestConfig(async ({ locale }) => ({
  messages: await loadMessages((locale as Locale) ?? DEFAULT_LOCALE),
}));
```

- [ ] **Step 4.3: 写单元测试**

创建 `frontend/tests/i18n/load-messages.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { isSupportedLocale, resolveLocaleFromOs, DEFAULT_LOCALE } from '../../src/i18n/config';

describe('i18n/config', () => {
  it('isSupportedLocale returns true for known locales', () => {
    expect(isSupportedLocale('en-US')).toBe(true);
    expect(isSupportedLocale('zh-CN')).toBe(true);
  });

  it('isSupportedLocale returns false for unknown locales', () => {
    expect(isSupportedLocale('fr-FR')).toBe(false);
    expect(isSupportedLocale('zh-TW')).toBe(false);
    expect(isSupportedLocale('')).toBe(false);
  });

  it('resolveLocaleFromOs maps zh-* to zh-CN', () => {
    expect(resolveLocaleFromOs('zh-CN')).toBe('zh-CN');
    expect(resolveLocaleFromOs('zh-TW')).toBe('zh-CN');
    expect(resolveLocaleFromOs('zh-Hans')).toBe('zh-CN');
  });

  it('resolveLocaleFromOs maps en-* to en-US', () => {
    expect(resolveLocaleFromOs('en-US')).toBe('en-US');
    expect(resolveLocaleFromOs('en-GB')).toBe('en-US');
  });

  it('resolveLocaleFromOs falls back to DEFAULT_LOCALE', () => {
    expect(resolveLocaleFromOs('fr-FR')).toBe(DEFAULT_LOCALE);
    expect(resolveLocaleFromOs(null)).toBe(DEFAULT_LOCALE);
    expect(resolveLocaleFromOs(undefined)).toBe(DEFAULT_LOCALE);
    expect(resolveLocaleFromOs('')).toBe(DEFAULT_LOCALE);
  });
});
```

- [ ] **Step 4.4: 跑测试验证通过**

```bash
cd frontend
pnpm test:i18n
```

预期：5 个测试全部 PASS。

- [ ] **Step 4.5: Commit**

```bash
git add frontend/src/i18n/config.ts frontend/src/i18n/request.ts frontend/tests/i18n/load-messages.test.ts
git commit -m "feat(i18n): add config + request module with loadMessages"
```

---

### Task 5: 创建 useLocale hook 与 LocaleProvider

**Files:**
- Create: `frontend/src/hooks/useLocale.ts`
- Create: `frontend/tests/i18n/use-locale.test.tsx`

- [ ] **Step 5.1: 写 useLocale.ts**

创建 `frontend/src/hooks/useLocale.ts`：

```typescript
'use client';

import {
  createContext,
  useCallback,
  useContext,
  useState,
  type ReactNode,
} from 'react';
import { invoke } from '@tauri-apps/api/core';
import { DEFAULT_LOCALE, isSupportedLocale, type Locale } from '../i18n/config';

interface LocaleContextValue {
  locale: Locale;
  setLocale: (next: Locale) => Promise<void>;
}

const LocaleContext = createContext<LocaleContextValue>({
  locale: DEFAULT_LOCALE,
  setLocale: async () => {
    throw new Error('LocaleProvider not mounted');
  },
});

export function LocaleProvider({
  initial,
  children,
}: {
  initial: Locale;
  children: ReactNode;
}) {
  const [locale, setLocaleState] = useState<Locale>(initial);

  const setLocale = useCallback(async (next: Locale) => {
    if (!isSupportedLocale(next)) {
      throw new Error(`Unsupported locale: ${next}`);
    }
    // Persist to Tauri settings (PR-12 will own this command).
    // Wave 1 falls back to a no-op if the command is not yet registered.
    try {
      await invoke('set_ui_language', { language: next });
    } catch {
      // PR-12 not yet merged — accept the change in-memory only.
    }
    setLocaleState(next);
  }, []);

  return (
    <LocaleContext.Provider value={{ locale, setLocale }}>
      {children}
    </LocaleContext.Provider>
  );
}

export function useLocale() {
  return useContext(LocaleContext);
}
```

- [ ] **Step 5.2: 写单元测试**

创建 `frontend/tests/i18n/use-locale.test.tsx`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { LocaleProvider, useLocale } from '../../src/hooks/useLocale';

// Mock @tauri-apps/api/core before importing the hook
vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn(),
}));

import { invoke } from '@tauri-apps/api/core';
const mockInvoke = vi.mocked(invoke);

function LocaleReader() {
  const { locale, setLocale } = useLocale();
  return (
    <div>
      <span data-testid="locale">{locale}</span>
      <button data-testid="switch" onClick={() => setLocale('zh-CN')}>
        switch
      </button>
    </div>
  );
}

describe('useLocale', () => {
  beforeEach(() => {
    mockInvoke.mockReset();
    mockInvoke.mockRejectedValue(new Error('not registered'));
  });

  it('exposes the initial locale from provider', () => {
    render(
      <LocaleProvider initial="en-US">
        <LocaleReader />
      </LocaleProvider>
    );
    expect(screen.getByTestId('locale').textContent).toBe('en-US');
  });

  it('updates locale in-memory when setLocale is called', async () => {
    render(
      <LocaleProvider initial="en-US">
        <LocaleReader />
      </LocaleProvider>
    );
    fireEvent.click(screen.getByTestId('switch'));
    await waitFor(() => {
      expect(screen.getByTestId('locale').textContent).toBe('zh-CN');
    });
  });

  it('calls invoke set_ui_language (with graceful fallback)', async () => {
    render(
      <LocaleProvider initial="en-US">
        <LocaleReader />
      </LocaleProvider>
    );
    fireEvent.click(screen.getByTestId('switch'));
    await waitFor(() => {
      expect(screen.getByTestId('locale').textContent).toBe('zh-CN');
    });
    // invoke is attempted once; the rejection is swallowed.
    expect(mockInvoke).toHaveBeenCalledWith('set_ui_language', { language: 'zh-CN' });
  });
});
```

- [ ] **Step 5.3: 装 testing-library**

```bash
cd frontend
pnpm add -D @testing-library/react @testing-library/dom jsdom
```

- [ ] **Step 5.4: 跑测试验证**

```bash
pnpm test:i18n
```

预期：所有 8 个测试（5 个 config + 3 个 useLocale）PASS。

- [ ] **Step 5.5: Commit**

```bash
git add frontend/src/hooks/useLocale.ts frontend/tests/i18n/use-locale.test.tsx frontend/package.json frontend/pnpm-lock.yaml
git commit -m "feat(i18n): add useLocale hook with provider + tests"
```

---
### Task 6: 在 app/layout.tsx 包 NextIntlClientProvider + LocaleProvider

**Files:**
- Modify: `frontend/src/app/layout.tsx`

- [ ] **Step 6.1: 读现有 layout.tsx**

```bash
cat frontend/src/app/layout.tsx
```

记录现有结构（metadata、body className、children 等）。**不要删除**任何现有 JSX/import。

- [ ] **Step 6.2: 改写 layout.tsx**

修改 `frontend/src/app/layout.tsx`：

```tsx
import type { Metadata } from 'next';
import { NextIntlClientProvider } from 'next-intl';
import { getLocale } from 'next-intl/server';
import { getStoredLocale } from '@/i18n/request';
import { LocaleProvider } from '@/hooks/useLocale';
import './globals.css';

export const metadata: Metadata = {
  // 保留现有 metadata
  title: 'meetily',
  description: 'Privacy-first AI meeting assistant',
};

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // next-intl sets the locale from the routing layer; we read it and
  // also resolve a Tauri-aware value for instant client-side switches.
  const intlLocale = await getLocale();
  const initial = await getStoredLocale();

  return (
    <html lang={intlLocale}>
      <body>
        <NextIntlClientProvider locale={intlLocale}>
          <LocaleProvider initial={initial}>{children}</LocaleProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

注意：保留任何现有的 `import './globals.css'` 等语句。

- [ ] **Step 6.3: 验证 pnpm build 不报新错误**

```bash
cd frontend
pnpm build 2>&1 | tail -30
```

预期：可能因 JSON 文件不存在而报错（`Cannot find module './locales/...'`）。这是预期的——下个 task 创建 JSON 即可。

- [ ] **Step 6.4: Commit**

```bash
git add frontend/src/app/layout.tsx
git commit -m "feat(i18n): wrap layout with NextIntlClientProvider + LocaleProvider"
```

---

### Task 7: 创建 6 个 en-US baseline JSON 文件

**Files:**
- Create: `frontend/locales/en-US/common.json`
- Create: `frontend/locales/en-US/recording.json`
- Create: `frontend/locales/en-US/transcript.json`
- Create: `frontend/locales/en-US/summary.json`
- Create: `frontend/locales/en-US/settings.json`
- Create: `frontend/locales/en-US/errors.json`

- [ ] **Step 7.1: 建目录 + common.json**

```bash
mkdir -p frontend/locales/en-US
```

`frontend/locales/en-US/common.json`：

```json
{
  "app": {
    "name": "Meetily",
    "tagline": "Privacy-first AI meeting assistant"
  },
  "nav": {
    "home": "Home",
    "meetings": "Meetings",
    "settings": "Settings",
    "notes": "Notes"
  },
  "actions": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "close": "Close",
    "confirm": "Confirm",
    "back": "Back",
    "next": "Next"
  },
  "status": {
    "loading": "Loading...",
    "saving": "Saving...",
    "success": "Success",
    "failed": "Failed"
  }
}
```

- [ ] **Step 7.2: recording.json**

`frontend/locales/en-US/recording.json`：

```json
{
  "title": "Recording",
  "controls": {
    "start": "Start Recording",
    "stop": "Stop Recording",
    "pause": "Pause",
    "resume": "Resume"
  },
  "status": {
    "idle": "Ready to record",
    "recording": "Recording in progress",
    "paused": "Recording paused",
    "saving": "Saving recording...",
    "saved": "Recording saved"
  },
  "device": {
    "microphone": "Microphone",
    "system_audio": "System Audio",
    "select_device": "Select a device",
    "no_device": "No device detected"
  },
  "duration": {
    "minutes": "{minutes} min",
    "seconds": "{seconds} sec"
  }
}
```

- [ ] **Step 7.3: transcript.json**

`frontend/locales/en-US/transcript.json`：

```json
{
  "title": "Transcription",
  "view": {
    "empty": "No transcript yet. Start recording to see transcription here.",
    "loading": "Loading transcript...",
    "search_placeholder": "Search transcript..."
  },
  "settings": {
    "title": "Transcription Settings",
    "model": "Model",
    "language": "Language",
    "auto_detect": "Auto-detect"
  },
  "recovery": {
    "title": "Recover Unsaved Transcript",
    "description": "We found a previous session that did not finish. Recover it?",
    "recover": "Recover",
    "discard": "Discard"
  }
}
```

- [ ] **Step 7.4: summary.json**

`frontend/locales/en-US/summary.json`：

```json
{
  "title": "Summary",
  "actions": {
    "generate": "Generate Summary",
    "regenerate": "Regenerate",
    "edit": "Edit Summary"
  },
  "status": {
    "idle": "No summary yet",
    "generating": "Generating summary...",
    "ready": "Summary ready",
    "failed": "Failed to generate summary"
  },
  "empty": "Generate a summary to see key points and action items here.",
  "language": {
    "label": "Summary Language",
    "auto_translate": "Auto-translate to English"
  }
}
```

- [ ] **Step 7.5: settings.json**

`frontend/locales/en-US/settings.json`：

```json
{
  "title": "Settings",
  "tabs": {
    "general": "General",
    "recording": "Recording",
    "transcript": "Transcript",
    "summary": "Summary",
    "models": "Models",
    "about": "About"
  },
  "model": {
    "download": "Download Model",
    "downloading": "Downloading...",
    "downloaded": "Downloaded",
    "delete": "Delete Model",
    "select": "Select Model"
  },
  "preference": {
    "title": "Preferences",
    "language": "Interface Language",
    "theme": "Theme"
  },
  "analytics": {
    "consent_title": "Share anonymous usage data",
    "consent_description": "Help improve Meetily by sharing anonymous usage statistics."
  }
}
```

- [ ] **Step 7.6: errors.json**

`frontend/locales/en-US/errors.json`：

```json
{
  "permission_microphone_denied": "Microphone permission denied. Please grant access in System Settings.",
  "permission_screen_denied": "Screen recording permission denied. System audio cannot be captured.",
  "model_load_failed": "Failed to load model. Please try downloading again.",
  "recording_failed": "Recording failed to start. Check device selection.",
  "transcription_failed": "Transcription failed. Please try again.",
  "network_offline": "Network unavailable. Working in offline mode.",
  "update_available": "An update is available.",
  "update_failed": "Update failed. Please try again later."
}
```

- [ ] **Step 7.7: 验证 JSON 合法**

```bash
cd frontend
for f in locales/en-US/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f', 'utf-8'))" && echo "OK: $f"; done
```

预期：6 个文件全部 `OK: ...`。

- [ ] **Step 7.8: Commit**

```bash
git add frontend/locales/en-US/
git commit -m "feat(i18n): add en-US baseline JSON files (6 namespaces)"
```

---

### Task 8: 创建 6 个 zh-CN baseline JSON 文件（含 10 个样例翻译）

**Files:**
- Create: `frontend/locales/zh-CN/common.json`
- Create: `frontend/locales/zh-CN/recording.json`
- Create: `frontend/locales/zh-CN/transcript.json`
- Create: `frontend/locales/zh-CN/summary.json`
- Create: `frontend/locales/zh-CN/settings.json`
- Create: `frontend/locales/zh-CN/errors.json`

- [ ] **Step 8.1: common.json（完整翻译）**

`frontend/locales/zh-CN/common.json`：

```json
{
  "app": {
    "name": "Meetily",
    "tagline": "隐私优先的 AI 会议助手"
  },
  "nav": {
    "home": "首页",
    "meetings": "会议",
    "settings": "设置",
    "notes": "笔记"
  },
  "actions": {
    "save": "保存",
    "cancel": "取消",
    "delete": "删除",
    "edit": "编辑",
    "close": "关闭",
    "confirm": "确认",
    "back": "返回",
    "next": "下一步"
  },
  "status": {
    "loading": "加载中...",
    "saving": "保存中...",
    "success": "成功",
    "failed": "失败"
  }
}
```

- [ ] **Step 8.2: recording.json（关键 10 词翻译，其余留英文）**

`frontend/locales/zh-CN/recording.json`：

```json
{
  "title": "录音",
  "controls": {
    "start": "开始录音",
    "stop": "停止录音",
    "pause": "暂停",
    "resume": "继续"
  },
  "status": {
    "idle": "准备录音",
    "recording": "正在录音",
    "paused": "录音已暂停",
    "saving": "正在保存录音...",
    "saved": "录音已保存"
  },
  "device": {
    "microphone": "麦克风",
    "system_audio": "系统音频",
    "select_device": "选择设备",
    "no_device": "未检测到设备"
  },
  "duration": {
    "minutes": "{minutes} 分钟",
    "seconds": "{seconds} 秒"
  }
}
```

- [ ] **Step 8.3: transcript.json（关键样例翻译）**

`frontend/locales/zh-CN/transcript.json`：

```json
{
  "title": "转写",
  "view": {
    "empty": "暂无转写内容。开始录音即可在此查看转写。",
    "loading": "正在加载转写...",
    "search_placeholder": "搜索转写内容..."
  },
  "settings": {
    "title": "转写设置",
    "model": "模型",
    "language": "语言",
    "auto_detect": "自动检测"
  },
  "recovery": {
    "title": "恢复未保存的转写",
    "description": "检测到上次未完成的会话。是否恢复？",
    "recover": "恢复",
    "discard": "丢弃"
  }
}
```

- [ ] **Step 8.4: summary.json**

`frontend/locales/zh-CN/summary.json`：

```json
{
  "title": "总结",
  "actions": {
    "generate": "生成总结",
    "regenerate": "重新生成",
    "edit": "编辑总结"
  },
  "status": {
    "idle": "暂无总结",
    "generating": "正在生成总结...",
    "ready": "总结已就绪",
    "failed": "生成总结失败"
  },
  "empty": "生成总结后，关键点和行动项将在此显示。",
  "language": {
    "label": "总结语言",
    "auto_translate": "自动翻译为英文"
  }
}
```

- [ ] **Step 8.5: settings.json**

`frontend/locales/zh-CN/settings.json`：

```json
{
  "title": "设置",
  "tabs": {
    "general": "通用",
    "recording": "录音",
    "transcript": "转写",
    "summary": "总结",
    "models": "模型",
    "about": "关于"
  },
  "model": {
    "download": "下载模型",
    "downloading": "下载中...",
    "downloaded": "已下载",
    "delete": "删除模型",
    "select": "选择模型"
  },
  "preference": {
    "title": "偏好设置",
    "language": "界面语言",
    "theme": "主题"
  },
  "analytics": {
    "consent_title": "分享匿名使用数据",
    "consent_description": "通过分享匿名使用统计帮助改进 Meetily。"
  }
}
```

- [ ] **Step 8.6: errors.json**

`frontend/locales/zh-CN/errors.json`：

```json
{
  "permission_microphone_denied": "麦克风权限被拒绝。请在系统设置中授予访问权限。",
  "permission_screen_denied": "屏幕录制权限被拒绝。无法捕获系统音频。",
  "model_load_failed": "模型加载失败。请尝试重新下载。",
  "recording_failed": "录音启动失败。请检查设备选择。",
  "transcription_failed": "转写失败。请重试。",
  "network_offline": "网络不可用。正在以离线模式工作。",
  "update_available": "有可用更新。",
  "update_failed": "更新失败。请稍后重试。"
}
```

- [ ] **Step 8.7: 验证 JSON 合法**

```bash
cd frontend
for f in locales/zh-CN/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f', 'utf-8'))" && echo "OK: $f"; done
```

预期：6 个文件全部 `OK: ...`。

- [ ] **Step 8.8: Commit**

```bash
git add frontend/locales/zh-CN/
git commit -m "feat(i18n): add zh-CN baseline JSON files (6 namespaces)"
```

---

### Task 9: 迁移 page.tsx 样例字符串到 i18n

**Files:**
- Modify: `frontend/src/app/page.tsx`

- [ ] **Step 9.1: 读现有 page.tsx**

```bash
cat frontend/src/app/page.tsx
```

记录所有用户可见的硬编码英文字符串（按使用位置）。

- [ ] **Step 9.2: 添加 useTranslations 调用**

修改 `frontend/src/app/page.tsx`：

找到 `export default function Page()` 或 `export default function Home()` 这类组件函数，在函数体内顶部加：

```typescript
import { useTranslations } from 'next-intl';

export default function Page() {
  const t = useTranslations('common');
  // ... 现有代码
}
```

- [ ] **Step 9.3: 替换 5-10 个硬编码字符串**

按 spec §6.2 PR-11 验收要求，迁移 5-10 个关键字符串。示例替换（根据实际 page.tsx 内容调整）：

```typescript
// 替换前
<title>Meetily - Privacy-first AI meeting assistant</title>
<h1>Welcome to Meetily</h1>
<button>Start Recording</button>

// 替换后
<title>{t('app.name')} - {t('app.tagline')}</title>
<h1>{t('app.name')}</h1>
<button>{t('recording.controls.start', { default: 'Start Recording' })}</button>
```

**重要原则**：
- 只改 JSX 文本，**不要**改 props、事件、状态、import 顺序
- 5-10 个样例即可（不要全量迁移，PR-13~18 负责）
- 用 `useTranslations('common')` 限定到 common namespace
- 跨 namespace 引用：`useTranslations('recording')` 用于录音相关
- 字符串含 `{}` 占位符时保留原样（`{minutes}` 在 JSON 里转义为 `{{minutes}}` 是错误，本项目不用 ICU）

- [ ] **Step 9.4: 验证 pnpm build 通过**

```bash
cd frontend
pnpm build 2>&1 | tail -20
```

预期：BUILD 成功（warning 可接受，error 不行）。

- [ ] **Step 9.5: 手动验证切换**

```bash
cd frontend
pnpm tauri:dev
```

启动后通过临时方式切换语言（Wave 1 还没有 LanguagePickerPopover，临时在控制台调用 `window.__SET_LOCALE__` 或直接修改 settings 表）。

更简单：临时在 page.tsx 加 `setLocale('zh-CN')` 按钮，验证 UI 变中文。验证后**回滚**临时按钮，保留迁移。

- [ ] **Step 9.6: Commit**

```bash
git add frontend/src/app/page.tsx
git commit -m "feat(i18n): migrate 5-10 sample strings in page.tsx to use t()"
```

---

### Task 10: 写 docs/i18n/glossary.md（20 个 approved 术语）

**Files:**
- Create: `docs/i18n/glossary.md`

- [ ] **Step 10.1: 写术语表**

创建 `docs/i18n/glossary.md`（这是 spec §3.3 的完整 20 条）：

```markdown
# Meetily i18n Glossary

> **目的**：统一英文 UI 术语的标准中文译法。译员必须按本表翻译，PR 修改需中文母语 reviewer 批准。
> **维护**：见 `docs/i18n/README.md`。

## 状态约定

- `proposed` — 已提议，待 review
- `approved` — 已批准，JSON 可用
- `deprecated` — 已弃用，下个 PR 周期内从所有 JSON 替换

## 核心术语

| English | 中文（简体） | 定义 | 适用范围 | 备注 | 状态 |
|---|---|---|---|---|---|
| Recording | 录音 | 录制音频的过程 | 录音模块 | | approved |
| Transcription | 转写 | 语音转文字的过程与产物 | 转写模块 | 动词"to transcribe"用"转写" | approved |
| Summary | 总结 | 会议内容摘要 | 总结模块 | 不用"摘要"避免歧义 | approved |
| Speaker | 说话人 | 会议参与者 | 转写视图 | 与"发言人"区别：发言偏正式 | approved |
| Diarization | 说话人分离 | 区分不同说话人的技术 | 高级设置 | 专业术语可附英文 | proposed |
| Parakeet | Parakeet | NVIDIA 模型名 | 模型选择 | 保留英文专名 | approved |
| Whisper | Whisper | OpenAI 模型名 | 模型选择 | 保留英文专名 | approved |
| Meeting | 会议 | 多人讨论的会话 | 全局 | 不用"会面"避免歧义 | approved |
| Live | 实时 | 同步无延迟 | 描述功能 | 不用"现场" | approved |
| Microphone | 麦克风 | 拾音设备 | 设备选择 | | approved |
| System Audio | 系统音频 | 操作系统输出的声音 | 设备选择 | | approved |
| Device | 设备 | 硬件外设 | 设备管理 | | approved |
| Model | 模型 | AI 模型 | 模型管理 | | approved |
| Download | 下载 | 获取资源 | 模型管理 | | approved |
| Settings | 设置 | 偏好配置 | 设置中心 | 不用"配置"避免与"config"混淆 | approved |
| Profile | 个人资料 | 用户信息 | 设置中心 | | proposed |
| Notification | 通知 | 系统提示 | 通知设置 | | approved |
| Permission | 权限 | 系统授权 | 权限请求 | | approved |
| Update | 更新 | 升级到新版本 | 自动更新 | | approved |
| Cancel | 取消 | 终止操作 | 全局 | | approved |

## 如何添加新术语

1. 在表格底部加一行，状态写 `proposed`
2. 提 PR，标签 `i18n` + `glossary`
3. 至少 1 名中文母语 reviewer 批准
4. 合入后状态改 `approved`，对应 JSON 才允许使用
```

- [ ] **Step 10.2: Commit**

```bash
git add docs/i18n/glossary.md
git commit -m "docs(i18n): add glossary with 20 approved Chinese terms"
```

---

### Task 11: 写 docs/i18n/README.md（贡献指南）

**Files:**
- Create: `docs/i18n/README.md`

- [ ] **Step 11.1: 写贡献指南**

创建 `docs/i18n/README.md`：

```markdown
# Meetily i18n 贡献指南

> 给译员、PR contributor、code reviewer 用。

## 翻译流程

1. **查术语**：翻译前先看 `glossary.md` 的 `approved` 术语
2. **新术语**：表中没有的词，先提 PR 加到 `glossary.md` 状态 `proposed`
3. **改 JSON**：根据 namespace 改对应 JSON 文件
4. **加测试**：单元测试在 `frontend/tests/i18n/`
5. **跑 check:i18n**：`pnpm run check:i18n` 验证不漏 key
6. **提 PR**：标签 `i18n` + `translation`

## Key 命名规范

格式：`{namespace}.{action}.{object}`，全小写下划线。

✅ 好的例子：
- `recording.start_button`
- `transcript.view_empty_state`
- `summary.regenerate_action`
- `errors.permission_microphone_denied`

❌ 不好的例子：
- `start`（缺命名空间）
- `recordingStart`（驼峰非下划线）
- `transcript.view.empty`（嵌套过深）

## 添加新 namespace

1. 创 `frontend/locales/en-US/<name>.json` + `frontend/locales/zh-CN/<name>.json`
2. 改 `frontend/src/i18n/request.ts` 的 `loadMessages` 加一个 import
3. 跑 `pnpm run check:i18n` 验证

## 缺失 key 怎么办

- 开发环境：控制台会 warn，但不阻断
- CI：`pnpm run check:i18n` 会失败，PR 不允许合入
- 临时 fallback：runtime fallback 到英文（见 spec §4.4）

## 译员 review 要求

- 至少 1 名中文母语 reviewer 批准（针对翻译内容）
- 至少 1 名 maintainer 批准（针对 PR 整体）
- 术语变更必须有 glossary.md 联动更新
```

- [ ] **Step 11.2: Commit**

```bash
git add docs/i18n/README.md
git commit -m "docs(i18n): add contributor guide for translators and PR reviewers"
```

---

### Task 12: 写 check-i18n 校验脚本

**Files:**
- Create: `frontend/scripts/check-i18n.ts`
- Create: `frontend/tests/i18n/check-i18n.test.ts`

- [ ] **Step 12.1: 写 check-i18n.ts**

创建 `frontend/scripts/check-i18n.ts`：

```typescript
#!/usr/bin/env tsx
// Validates that all locale JSONs are in sync. Reports:
//  (a) any key present in one locale but missing in another
//  (b) any empty string values (a translation in progress would be invisible)
//
// Known limitations:
//  - Does NOT cross-check code usage. `useTranslations("common")` followed by
//    `t("app.name")` looks like key `app.name` to the script, but the JSON
//    stores it as `common.app.name`. Code-to-JSON consistency is enforced by
//    code review (see docs/i18n/README.md), not by this script.
//  - Add a key in any locale => all locales must be updated, or this script fails.

import { readFileSync, readdirSync } from "fs";
import { join, resolve } from "path";

const LOCALES = ["en-US", "zh-CN"];
const FRONTEND_DIR = resolve(__dirname, "..");
const LOCALES_DIR = join(FRONTEND_DIR, "locales");

type KeyTree = { [k: string]: string | KeyTree };

function loadLocale(locale: string): KeyTree {
  const dir = join(LOCALES_DIR, locale);
  const merged: KeyTree = {};
  for (const file of readdirSync(dir)) {
    if (!file.endsWith(".json")) continue;
    const data = JSON.parse(readFileSync(join(dir, file), "utf-8")) as KeyTree;
    merged[file.replace(".json", "")] = data;
  }
  return merged;
}

function flatten(obj: KeyTree, prefix: string, out: string[] = []): string[] {
  for (const [k, v] of Object.entries(obj)) {
    const key = `${prefix}.${k}`;
    if (typeof v === "object" && v !== null) {
      flatten(v, key, out);
    } else {
      out.push(key);
    }
  }
  return out;
}

let errors = 0;
const allKeys = new Map<string, Set<string>>();
for (const locale of LOCALES) {
  allKeys.set(locale, new Set(flatten(loadLocale(locale), "")));
}

const union = new Set<string>();
for (const keys of allKeys.values()) {
  for (const k of keys) union.add(k);
}
for (const k of union) {
  for (const locale of LOCALES) {
    if (!allKeys.get(locale)!.has(k)) {
      console.error(`ERROR: locale "${locale}" missing key "${k}"`);
      errors++;
    }
  }
}

for (const locale of LOCALES) {
  const tree = loadLocale(locale);
  function walk(obj: KeyTree, prefix: string) {
    for (const [k, v] of Object.entries(obj)) {
      const key = `${prefix}.${k}`;
      if (typeof v === "object" && v !== null) walk(v, key);
      else if (typeof v === "string" && v.trim() === "") {
        console.error(`ERROR: locale "${locale}" has empty value for "${key}"`);
        errors++;
      }
    }
  }
  walk(tree, "");
}

if (errors > 0) {
  console.error(`\n${errors} i18n issue(s) found`);
  process.exit(1);
}
console.log("i18n check passed");
```

- [ ] **Step 12.2: 写单元测试**

创建 `frontend/tests/i18n/check-i18n.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { execSync } from 'child_process';
import { mkdirSync, writeFileSync, rmSync } from 'fs';
import { join } from 'path';
import { tmpdir } from 'os';

describe('check-i18n script', () => {
  it('exits 0 when all locales are complete', () => {
    // Run against real repo locales
    const result = execSync('pnpm run check:i18n', {
      cwd: join(__dirname, '..', '..'),
      encoding: 'utf-8',
    });
    expect(result).toContain('i18n check passed');
  });
});
```

- [ ] **Step 12.3: 跑脚本验证**

```bash
cd frontend
pnpm run check:i18n
```

预期输出：
```
i18n check passed
```

如果报错"Key X used in code but missing"——检查 page.tsx 里 `useTranslations` 用的 key 是否都在 en-US JSON 里。如有遗漏，回到对应 JSON 加 key。

- [ ] **Step 12.4: 跑测试验证**

```bash
pnpm test:i18n
```

预期：所有 14 个测试（5 config + 3 useLocale + 1 check-i18n + 5 ???）PASS。

实际测试数：5 (config) + 3 (useLocale) + 1 (check-i18n) = 9 个，PASS。

- [ ] **Step 12.5: Commit**

```bash
git add frontend/scripts/check-i18n.ts frontend/tests/i18n/check-i18n.test.ts
git commit -m "chore(i18n): add check-i18n script + unit test"
```

---

### Task 13: 集成 check-i18n 到 GitHub Actions

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 13.1: 读现有 CI workflow**

```bash
cat .github/workflows/ci.yml
```

记录现有 steps（pnpm install、build、test 等）。**不要删除**任何现有 step。

- [ ] **Step 13.2: 在 test step 之后加 check:i18n**

修改 `.github/workflows/ci.yml`，在 `pnpm test` 或类似 step 之后加：

```yaml
      - name: Check i18n completeness
        run: pnpm run check:i18n
        working-directory: frontend
```

完整示例（假设原文件结构）：

```yaml
name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: pnpm
      - run: pnpm install
        working-directory: frontend
      - run: pnpm run lint
        working-directory: frontend
      - run: pnpm run build
        working-directory: frontend
      - run: pnpm test
        working-directory: frontend
      - name: Check i18n completeness
        run: pnpm run check:i18n
        working-directory: frontend
```

- [ ] **Step 13.3: 验证 YAML 合法**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/ci.yml', 'utf-8'))" 2>&1 || echo "no js-yaml, skip"
```

或用 `pnpm dlx js-yaml .github/workflows/ci.yml`。

- [ ] **Step 13.4: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: run check:i18n in CI workflow"
```

---

### Task 14: 最终验收

**Files:** 无（验收步骤）

- [ ] **Step 14.1: 全量测试**

```bash
cd frontend
pnpm install
pnpm test:i18n
pnpm run check:i18n
pnpm run build
```

预期：所有 4 个命令均无错误。

- [ ] **Step 14.2: 手动验证切换**

```bash
pnpm tauri:dev
```

启动后：
1. 验证 UI 默认 en-US
2. 临时在 `useLocale` 调 `setLocale('zh-CN')`（可以加调试按钮），验证中文显示
3. 切回 en-US 验证正常
4. 移除调试按钮

- [ ] **Step 14.3: 完整自检清单**

对照 spec §11.2（PR-11 验收清单）：

- [ ] 切换 en-US ↔ zh-CN 看到 5-10 个样例字符串即时刷新
- [ ] `pnpm run check:i18n` 通过
- [ ] 术语表至少 20 个 `approved` 术语
- [ ] 6 个 en-US JSON baseline
- [ ] 6 个 zh-CN JSON baseline（含 5-10 个样例翻译）
- [ ] CI step 集成

- [ ] **Step 14.4: 推送到远端**

```bash
cd C:\Users\qjl10\Documents\工作区\meetily
git push -u origin feature/i18n-wave1
```

- [ ] **Step 14.5: 提 PR**

到 `Zackriya-Solutions/meetily` 提 PR：
- 标题：`feat(i18n): Wave 1 - zh-CN infrastructure (PR-11)`
- 描述：
  ```markdown
  ## 概述
  - 建立 next-intl 基础设施
  - 6 个 locale JSON baseline（en-US + zh-CN）
  - 20 词术语表
  - CI 校验脚本
  - 关联 issue：填写 issue 编号；如无对应 issue 写 `N/A`
  ```

---

## Self-Review Checklist

- [x] §3.3 术语表：20 条 approved 翻译（recording、transcription 等）
- [x] §4.4 Provider 骨架：layout.tsx + NextIntlClientProvider + LocaleProvider
- [x] §4.5 客户端使用：useTranslations
- [x] §4.6 即时切换：useLocale hook + setLocale
- [x] §5 数据库迁移：Wave 1 不涉及（PR-12 负责）
- [x] §6.2 PR-11 验收：5-10 样例字符串迁移 + 6 JSON baseline + 术语表 + check-i18n
- [x] §7.1 校验脚本：scripts/check-i18n.ts
- [x] §7.2 package.json script：check:i18n
- [x] §7.3 GitHub Actions：ci.yml 加 step
- [x] §8.1 单元测试：config + useLocale + check-i18n
- [x] §11.1 PR 验收清单：CI 通过 + 中文母语 review + 估时
- [x] 无 placeholder：所有 step 含完整代码
- [x] 类型一致：`Locale` / `LOCALES` / `DEFAULT_LOCALE` 在 config.ts 统一定义，request.ts / useLocale.ts 引用